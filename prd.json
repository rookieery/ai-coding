{
  "branchName": "feature/chinese-chess-migration",
  "stories": [
    {
      "id": "CHESS-PLAY-002-AgentChessPanel",
      "title": "前端：创建 AgentChessPanel.vue 中国象棋分屏对弈面板",
      "description": "核心组件开发。在 `gomoku-web/src/components/agent/` 下新建 `AgentChessPanel.vue`。该组件是 Agent 分屏对弈模式中右侧的中国象棋交互面板，类比 `AgentGomokuPanel.vue` 的设计模式。\n\n1. **棋盘渲染**：引入中国象棋 Board 组件（`src/games/chinese-chess/components/Board.vue`），渲染 10×9 棋盘。使用 `useGameState` composable 管理内部棋盘状态（初始化为标准开局）。\n\n2. **玩家交互**：监听 Board 的棋子点击和落子事件。仅当轮到玩家行棋时允许操作（红方默认为玩家）。当轮到 AI 时禁用棋盘点击。\n\n3. **走法通知**：玩家落子后，emit `@userMove` 事件，携带走法信息 `{ from: { row, col }, to: { row, col }, notation: string }`。notation 使用现有的中国象棋记谱法工具（`src/games/chinese-chess/notation.ts`）生成标准记谱（如「炮二平五」）。\n\n4. **AI 落子接口**：暴露 `placeAiPiece(from: { row: number; col: number }, to: { row: number; col: number }): void` 方法。外部调用此方法在棋盘上执行 AI 的走法，触发内部状态更新（移动棋子、处理吃子、切换行棋方），然后 emit `@aiMove` 事件。\n\n5. **状态查询**：暴露 `getBoard(): number[][]` 方法返回当前棋盘的数值矩阵（通过 `convertBoardStateToCodes` 转换）。暴露 `getMoveHistory(): ChessMoveRecord[]` 返回走法历史。暴露 `isValidMove(from, to): boolean` 校验走法合法性。暴露 `getCurrentPlayer(): 'red' | 'black'` 获取当前行棋方。\n\n6. **游戏控制**：暴露 `resetGame(): void` 重置棋盘到标准开局。暴露 `undoLastMove(): void` 悔棋（撤回最后一步）。\n\n7. **游戏结束检测**：每次走法后检测是否将死或困毙。如果游戏结束，emit `@gameOver` 事件，携带 `{ winner: 'red' | 'black' | 'draw', reason: string }`。\n\n8. **UI 布局**：面板顶部显示游戏状态（当前行棋方、回合数）。底部提供紧凑的控制按钮（悔棋、新局、认输）。棋盘居中占满面板主体区域，适配容器宽度。\n\n9. **AI 角色选择**：接受 prop `playerSide: 'red' | 'black'`，默认 `'red'`。决定哪方由玩家操作，哪方由 AI 控制。\n\nProps：`playerSide?: 'red' | 'black'`（默认 `'red'`）。\nEmits：`userMove(move)`、`aiMove(move)`、`gameOver(result)`。\nDefineExpose：`placeAiPiece`、`getBoard`、`getMoveHistory`、`isValidMove`、`getCurrentPlayer`、`resetGame`、`undoLastMove`。\n\n所有 UI 文本使用 i18n。预估代码约 350 行。",
      "passes": true
    },
    {
      "id": "CHESS-PLAY-003-MoveParser",
      "title": "前端：编写中国象棋聊天走法解析器",
      "description": "纯逻辑层任务。在 `gomoku-web/src/games/chinese-chess/` 下新建 `moveParser.ts`。\n\n实现 `parseChessMoveText(text: string, board: number[][]): ParsedChessMove | null` 函数，用于在棋林对弈模式中解析用户通过聊天输入框输入的走法指令。\n\n**支持的输入格式**：\n1. **标准记谱法**（如「炮二平五」「马八进七」「车一进一」）：使用正则匹配 `([车馬马炮兵卒仕士相象帅将])([一二三四五六七八九123456789])([进退平])([一二三四五六七八九123456789])` 模式。根据棋子类型、列号、方向（进/退/平）、目标位置推算出 from/to 坐标。需区分红黑方的数字映射（红方从右到左为一至九，黑方从右到左为 1 至 9）。\n\n2. **坐标格式**（如「0,0-1,0」或「(0,0)(1,0)」）：使用正则匹配 `\\(?\\s*(\\d)\\s*,\\s*(\\d)\\s*\\)?\\s*[-到至>]\\s*\\(?\\s*(\\d)\\s*,\\s*(\\d)\\s*\\)?`。直接提取 from(row,col) 和 to(row,col)。\n\n3. **简写格式**（如「进车」「跳马」等模糊描述）：作为可选的扩展解析，如果前两种格式均未匹配，尝试匹配常见动词+棋子的组合，返回可能的走法候选列表（交由上层逻辑选择）。此阶段可暂不实现，返回 null 即可。\n\n**返回类型** `ParsedChessMove`：`{ from: { row: number; col: number }; to: { row: number; col: number }; notation?: string; confidence: number }`。confidence 为解析置信度（标准记谱法=1.0，坐标格式=0.9，模糊匹配=0.5）。\n\n**校验**：解析出的坐标必须满足 0 ≤ row ≤ 9、0 ≤ col ≤ 8，from 位置必须有对应棋子，走法需基本合法（可以不在意将军检测，由上层进一步校验）。\n\n预估代码约 200 行。",
      "passes": false
    },
    {
      "id": "CHESS-PLAY-004-AgentView-Layout",
      "title": "前端：AgentView 集成中国象棋分屏布局",
      "description": "视图层联调任务。修改 `gomoku-web/src/views/AgentView.vue`。\n\n**第一部分：布局与渲染**\n1. 引入 `AgentChessPanel` 组件。\n2. 修改 `isSplitLayout` 计算属性，增加 `playMode === 'chinese-chess'` 条件，使中国象棋对弈也触发左右分屏布局。\n3. 在右侧面板区域（template 中渲染 `AgentGomokuPanel` 和 `AgentChessVisionPanel` 的同级位置），增加 `v-else-if=\"playMode === 'chinese-chess'\"` 分支，渲染 `AgentChessPanel`。\n4. 确保拖拽分割线、返回按钮在中国象棋对弈模式下同样生效（相关 v-if 条件增加 `'chinese-chess'` 判断）。\n5. 为 `AgentChessPanel` 绑定事件监听：`@userMove`、`@aiMove`、`@gameOver`。\n\n**第二部分：游戏选择器更新**\n1. 修改 `handleGameSelection(gameType, msg)` 方法，增加 `gameType === 'chinese-chess'` 分支。\n2. 当用户选择中国象棋时：调用 `useGlobalAgentPlay().enterChessMode()` 进入分屏对弈模式（而非 `enterGomokuMode()`）。\n3. 向 messages 数组推送提示消息，使用 i18n key `chessPlayModeEntered`（如「已进入中国象棋对弈模式，执红先行，请落子」）。\n4. 调用 `chatMessagesRef.value?.scrollToBottom()` 滚动到底部。\n\n**第三部分：游戏选择器按钮启用**\n1. 在 `AgentChatMessages.vue` 中找到渲染游戏选择按钮的位置（当前中国象棋按钮是置灰禁用状态，标注「即将推出」）。\n2. 移除禁用状态和高亮样式，使中国象棋按钮变为可点击的正常状态。\n3. 修改按钮文本移除「(即将推出)」后缀。\n4. 绑定点击事件 emit `select-game` 并传递 `'chinese-chess'`。\n\n预估代码变更约 60 行。",
      "passes": false
    },
    {
      "id": "CHESS-PLAY-005-UserMove-Flow",
      "title": "前端：实现中国象棋用户落子拦截与 AI 思考触发",
      "description": "数据桥接联调任务。修改 `gomoku-web/src/views/AgentView.vue`。\n\n1. **用户落子事件处理**：实现 `handleChessUserMove(move)` 方法，绑定到 `AgentChessPanel` 的 `@userMove` 事件。\n   a. 在左侧聊天流追加系统消息：「玩家行棋：{move.notation}」（使用 i18n key `chessUserMoveMsg`，参数为 notation）。\n   b. 将 `useGlobalAgentPlay().isAIThinking` 设为 true，锁定 UI（禁用棋盘点击）。\n   c. 从 `AgentChessPanel` 获取当前棋盘状态：`chessPanelRef.value.getBoard()` 和 `chessPanelRef.value.getCurrentPlayer()`。\n   d. 构造 `ChessLLMMoveRequest`，调用 `chessLlmApi.generateChessLLMMove(request)` 发起后端 LLM 走子请求。\n\n2. **AI 响应处理**：在上述请求的回调中：\n   a. 如果请求成功：提取响应中的 `move.from/to`，调用 `chessPanelRef.value.placeAiPiece(move.from, move.to)` 在棋盘上执行 AI 走法。\n   b. 在左侧聊天流追加 AI 消息，包含 `reason`（AI 的分析文本）和 `situationAnalysis`（局势评估），使用 `ThinkingProcess.vue` 的可折叠 UI 展示思考过程。\n   c. 将 `isAIThinking` 设为 false，解锁 UI。\n   d. 如果 AI 走法导致将军，追加提示消息（使用 i18n key `chessCheckMsg`）。\n\n3. **错误处理**：如果 LLM 请求失败，在聊天流追加错误提示消息，`isAIThinking` 设为 false，告知用户 AI 暂时无法响应。\n\n4. **游戏结束处理**：实现 `handleChessGameOver(result)` 方法，绑定到 `@gameOver` 事件。\n   a. 在聊天流追加游戏结束消息（使用 i18n key `chessCheckmateMsg`，参数为获胜方）。\n   b. `isAIThinking` 设为 false。\n\n预估代码变更约 100 行。",
      "passes": false
    },
    {
      "id": "CHESS-PLAY-006-ChatMove-Interceptor",
      "title": "前端：AgentView 聊天输入拦截与象棋走法解析分流",
      "description": "核心联调任务。修改 `gomoku-web/src/views/AgentView.vue` 的 `handleSend` 方法。\n\n1. 当 `playMode === 'chinese-chess'` 时，获取用户输入 `query.value`，先调用 `parseChessMoveText(text, chessPanelRef.value.getBoard())` 尝试解析为象棋走法。\n\n2. **走法解析成功**（返回非 null）：\n   a. 通过 `chessPanelRef.value.isValidMove(parsedMove.from, parsedMove.to)` 校验走法合法性。\n   b. 如果合法：将用户消息上屏展示（格式为「玩家行棋：{notation}」），清空输入框，然后触发与棋盘点击落子相同的流程（调用 `handleChessUserMove` 的核心逻辑）。\n   c. 如果不合法：直接在聊天流追加 Agent 提示消息，使用 i18n key `chessInvalidMoveMsg`，告知用户走法不合法，不触发 AI 思考。\n\n3. **走法解析失败**（返回 null）：说明用户在进行普通聊天（非走法指令），直接走原有的 `sendMessage` / `executeStreamingChat` 逻辑，将消息发送给 DeepSeek Reasoner 进行普通对话。\n\n4. 确保与现有的五子棋走法解析逻辑（`parseMoveText`）互不冲突，两个分支各自独立处理。\n\n预估代码变更约 40 行。",
      "passes": false
    },
    {
      "id": "CHESS-PLAY-007-Smoke-Test",
      "title": "全链路冒烟测试与功能验证",
      "description": "测试验证任务。启动前端和后端开发服务器，手动验证以下路径：\n\n**Feature 1：中国象棋神经网络 AI（独立页面）**：\n1. 进入中国象棋独立页面 → 选择难度「神经网络(DeepSeek)」→ 开始 PvE 对弈 → 落下一子 → 观察前端显示「DeepSeek 正在推演局势...」→ 等待 AI 走法 → 确认 AI 走法合法且附有分析文本 → 继续对弈多回合验证稳定性。\n2. 测试 AI 在被将军时的应将行为 → 确认威胁检测器正确拦截。\n3. 测试 AI 在存在绝杀时的直接杀棋行为 → 确认无需调用 LLM。\n\n**Feature 2：棋林对弈（智能体分屏）**：\n4. 进入智能体页面 → 点击「中国象棋」按钮 → 确认分屏布局正确渲染 → 右侧显示标准象棋开局棋盘。\n5. 点击棋盘落子 → 确认左侧聊天流显示走法记谱 → 确认 AI 开始思考 → 确认 AI 走法在棋盘上正确渲染 → 确认聊天流显示 AI 分析文本。\n6. 在聊天输入框输入「炮二平五」→ 确认走法被正确解析并执行 → 确认 AI 正常响应。\n7. 在聊天输入框输入普通文本（如「你好」）→ 确认消息走普通聊天流程，不影响棋局。\n8. 测试悔棋、新局、认输功能是否正常。\n9. 测试将死/困毙检测 → 确认游戏结束消息正确显示。\n\n**回归验证**：\n10. 五子棋对弈流程不受影响（游戏选择器、分屏布局、走法解析均无回归）。\n11. 五子棋/象棋视觉识别流程不受影响。\n12. 普通聊天流程不受影响。",
      "passes": false
    }
  ]
}