# Ralph Progress Log
Started: Fri Apr 10 19:52:09     2026










































































ONLINE-21-ELO-Service: 后端 ELO 积分计算服务 + 单元测试 — 新增 2 个文件（elo.service.ts 168 行 + elo.service.test.ts 229 行）。(1) EloService 纯函数：calculateNewRating(playerRating, opponentRating, result, playerGamesPlayed, opponentGamesPlayed) 实现标准 ELO 公式 Expected = 1/(1+10^((opp-player)/400))，K-factor 动态调整（前30局K=40, rating<2400 K=20, rating>=2400 K=10），Math.round 取整 + Math.max(0, ...) 下限保护。updateRatings(winnerId, loserId, isDraw) 异步方法：查询双方 User.rating 和历史对局数（Match count），调用 calculateNewRating 计算新积分，并行更新数据库。(2) 24 个单元测试覆盖 7 个场景：初始积分对弈（1200 vs 1200）、高分胜低分（小变化）、低分胜高分（大变化/爆冷）、平局（双方向均值靠拢）、K-factor 动态变化（K=40/20/10 + 混合 K）、边界值（rating=0、极大值9999、负数钳制、loss 结果）、零和属性验证。TypeScript 编译通过，24 个 ELO 测试全部通过，2 个预存测试失败（candidateGenerator + llm-ai）与本次变更无关。更新 .docs/backend-technical.md 和 .docs/agent-file-map.md。 - 2026年05月17日
ONLINE-22-ELO-Integration: 后端 ELO 积分集成到游戏结束流程 + 积分 REST API — 新增 2 个文件（user.controller.ts 141 行 + user.routes.ts 21 行），修改 6 个文件（online-game.service.ts +155 行、disconnect.service.ts +16 行、game.handler.ts +3 行、socket/types.ts +9 行、routes/index.ts +2 行、.docs 3 个文档）。(1) A. 游戏结束积分更新：在 online-game.service.ts 新增 finalizeRankedGame(roomId, winner, isDraw) 公共方法，统一处理三种游戏结束路径（makeMove 胜利/平局、resign 认输、disconnect 断线超时）。方法内部：获取双方旧积分 → 调用 eloService.updateRatings → 创建 Match 记录（type='online', result='black'|'white'|'draw', playerBlackType/WhiteType='human', duration 秒数）→ 更新 Room.matchId 关联。MoveResult 和 ResignResult 接口新增可选 ratingChanges 字段。game:over Socket 广播携带 ratingChanges（含 black/white 各自 oldRating, newRating, change）。disconnect.service.ts 断线超时回调中新增排位赛判断，调用 finalizeRankedGame 并将 ratingChanges 传递到 game:over 广播（ELO 错误不阻断主流程，仅记录日志）。(2) B. 积分 REST API：新增 user.controller.ts + user.routes.ts，挂载在 /api/users。GET /api/users/:id/rating 返回用户积分信息（rating, totalGames, wins, losses, draws, winRate，通过 Match 表聚合计算，result='black'/'white'/'draw' 三种结果）。GET /api/users/leaderboard 返回 Top 50 排行榜（按 rating 倒序，含 rank 字段）。两个端点均使用 optionalAuthenticate 中间件。TypeScript 编译通过，24 个 ELO 测试全部通过，2 个预存测试失败与本次变更无关。更新 .docs/backend-technical.md、agent-file-map.md、agent-api-reference.md。 - 2026年05月17日

ONLINE-23-Matchmaking-Service: 后端匹配服务 + 单元测试 — 重写 matchmaking.service.ts（从 stub 37 行扩展到 177 行）+ 新增 matchmaking.service.test.ts（180 行，14 个测试）。(1) MatchmakingService 内存队列：Map<ruleMode, QueueEntry[]>，enqueue 防重复（isQueued 检查所有队列），dequeue 清理空队列，findMatch 动态阈值（基础 200 分，等待超 30s 后每 10s 扩大 100 分，选择最近积分对手），getQueuePosition（1-based），getQueueSize。(2) 14 个单元测试覆盖：相同积分立即匹配、积分差 >200 不匹配、等待超时阈值扩展匹配、多人最优配对（最近积分优先）、取消后不再匹配、空队列/单人无报错、防重复入队、不同 ruleMode 不交叉、队列位置追踪、连续匹配、阈值扩展精确时间行为。TypeScript 编译通过，14 个测试全部通过。更新 .docs/backend-technical.md 和 .docs/agent-file-map.md。 - 2026年05月17日ONLINE-24A-Matchmaking-Handler: 后端匹配 Socket Handler — 新增 match.handler.ts（197 行），修改 2 个文件（room.service.ts +1 行参数、socket/index.ts +3 行导入/注册/定时器）。(1) registerMatchHandlers(io, socket)：match:queue 处理 — 验证 socket.data.user → 查询用户 rating → 调用 matchmakingService.enqueue(userId, rating, ruleMode) → 回发 match:waiting { position } → 立即调用 findMatch() 尝试匹配；match:cancel 处理 — 调用 matchmakingService.dequeue(userId)。(2) handleMatchResult(io, match) 内部函数：并行查询双方用户信息（id, username, rating）→ 创建排位房间（isRanked=true）→ 对手自动 joinRoom → 查找双方 Socket 并加入 Socket.io room → 向双方发送 match:found { roomId, opponent }。(3) startMatchmakingTimer(io)：每 5 秒 setInterval 调用 findMatch()，匹配成功时调用 handleMatchResult。(4) roomService.createRoom 新增可选参数 isRanked（默认 false），创建房间时传入 isRanked 字段。(5) socket/index.ts 新增导入 registerMatchHandlers + startMatchmakingTimer，在 initializeSocket 中注册 handler 并启动定时器。TypeScript 编译通过，14 个匹配测试 + 24 个 ELO 测试全部通过。更新 .docs/backend-technical.md 和 .docs/agent-file-map.md。 - 2026年05月17日

ONLINE-24B-Frontend-Matchmaking: 前端匹配 composable + 匹配 UI 组件 — 新增 2 个文件（useMatchmaking.ts 131 行 + MatchmakingOverlay.vue 226 行），修改 4 个文件（OnlineLobbyView.vue +48 行、i18n.ts +13 行类型/新增翻译、zh-CN.json +5 行、en-US.json +5 行）。(1) A. useMatchmaking composable：单例共享状态（isMatching、queuePosition、matchedOpponent、matchedRoomId），startMatchmaking(ruleMode) 确保 Socket 已连接后发送 match:queue，cancelMatchmaking() 发送 match:cancel 并重置状态，navigateToRoom() 跳转到匹配房间。监听 match:waiting 更新队列位置，监听 match:found 设置对手信息和房间 ID。onUnmounted 时自动取消匹配并注销监听器。使用 listenerCount 引用计数模式防止重复注册/注销（与 useOnlineGame/useRoom 一致）。(2) B. MatchmakingOverlay.vue：全屏遮罩 + fade 过渡动画，搜索状态显示 Swords 图标脉冲动画 + 三个弹跳圆点 + 等待时间（computed 格式化为 mm:ss 或秒数）+ 队列位置徽章 + 取消按钮。匹配成功状态显示 VS 对阵界面（己方/对手头像 + 用户名 + 积分）+ 3 秒自动跳转倒计时。watch(visible) 启动/停止计时器，watch(isMatchFound) 触发 redirectHandle。(3) C. OnlineLobbyView.vue 集成：新增琥珀色「排位赛」按钮（Swords 图标）+ MatchmakingOverlay 组件，handleRankedMatch 校验登录后调用 startMatchmaking('standard')。(4) i18n 新增 5 个翻译 key（onlineMatchmakingQueuePosition、onlineMatchmakingWaitTime、onlineMatchFoundRedirecting、onlineMatchFoundVs、onlineMatchmakingSearching），同时补全了之前遗漏的 13 个在线对弈类型声明（navOnline、onlineLobby、onlineOpponentDisconnected 等）。TypeScript 编译通过（仅 2 个预存错误），Vite build 成功，184 个测试全部通过。更新 .docs/frontend-technical.md 和 .docs/agent-file-map.md。 - 2026年05月17日
