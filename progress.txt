# Ralph Progress Log
Started: Fri Apr 10 19:52:09     2026















LLM-Opt-002-Service-Interceptor: LLM服务层威胁拦截器集成。1. 在 `llm-ai.service.ts` 的 `generateMove` 方法中集成威胁检测：调用大模型前先执行 `findCriticalMove(board, currentPlayer)`。2. 若检测到关键坐标，直接返回本地响应，跳过DeepSeek API调用，响应格式 `{ move, reason, isFallback: true }`。3. 原因文案映射：必胜点/冲四堵截/活三堵截 三种场景。4. 只有当威胁检测返回null时才调用大模型API。5. TypeScript编译通过。 - 2026年04月25日
LLM-OPT-01-Advanced-Threats: 重构威胁检测器实现冲四与活三防御。1. 重构 `threatDetector.ts`：移除原有错误的 `checkLiveThree` 和 `checkRushFour` 函数，改用正确的模式检测逻辑。2. 新增 `detectOpponentLiveThree` 函数：检测对手活三(.OOO.)和间隙活三(.O.OO./.OO.O.)，返回堵截坐标。3. 新增 `detectOpponentRushFour` 函数：检测对手冲四(XOOOO./.OOOOX)、开放四(.OOOO.)、间隙四(O.OOO/OO.OO/OOO.O)，返回堵截坐标。4. 新增 `getLinePattern` 辅助函数：提取指定方向的棋子序列用于模式匹配。5. 优先级排序：必胜(100) > 冲四堵截(95) > 活三堵截(90)。6. 新增 `threatDetector.test.ts`：14个单元测试覆盖必胜检测、冲四堵截、活三堵截、优先级排序、数值棋盘格式转换。7. 新增 `jest.config.js`：配置ts-jest支持TypeScript测试。8. TypeScript编译通过，所有测试通过。 - 2026年04月25日
LLM-OPT-02-Candidate-Generator: 实现启发式候选落子生成器。1. 新建 `candidateGenerator.ts`：实现棋型评估算法，遍历棋盘所有空位计算攻防分值。2. 棋型定义：连五(100000) > 活四(10000) > 冲四(1000) > 活三(500) > 眠三(100) > 活二(50) > 眠二(10)。3. 核心函数：`generateCandidateMoves(board, aiColor, topN)` 返回Top N候选点数组，包含坐标、攻防分值、检测到的棋型列表、落子理由。4. 攻防评分：攻击分值按棋型基础分计算，防守分值按基础分*0.9计算（优先进攻）。5. 落子理由生成：根据检测到的棋型自动生成中文描述（如"必胜点：连五获胜"、"进攻点：构成活三"、"防守点：堵截对手活四"）。6. 辅助函数：`generateCandidateMovesFromNumeric` 支持数值棋盘格式(0/1/2)，`getBestFallbackMove` 返回最高分候选点用于兜底。7. 新增 `candidateGenerator.test.ts`：25个单元测试覆盖连五检测、活四/冲四/活三检测、防守检测、评分计算、多方向棋型、数值棋盘转换。8. TypeScript编译通过，所有测试通过。 - 2026年04月25日
