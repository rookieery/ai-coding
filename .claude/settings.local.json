{
  "permissions": {
    "allow": [
      "Read(//d/code/gomoku-training/**)",
      "Bash(python scripts/download_sgf_data.py --help)",
      "Bash(venv/Scripts/python.exe scripts/evaluate_model.py --model checkpoints/supervised/best.pth --dataset --num-samples 1000)"
    ],
    "additionalDirectories": [
      "d:\\code\\gomoku-training\\scripts"
    ]
  },
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if git diff --quiet .docs/; then echo 'Docs are up to date.'; else echo '检测到项目已修改，请执行 /sync-docs 更新索引。'; exit 2; fi"
          }
        ]
      }
    ]
  }
}
