# Ralph Progress Log
Started: Fri Apr 10 19:52:09     2026
---
2026-04-10 20:02 | S1 | 创建中国象棋类型定义文件 | PASS | 在 gomoku-web/src/chinese-chess/types.ts 中定义了完整的类型系统，包括棋子类型、玩家方、棋盘坐标、游戏状态、移动历史等接口，严格遵循 TypeScript 类型规范。
2026-04-10 20:10 | S2 | 实现棋盘初始化和状态管理 | PASS | 在 gomoku-web/src/chinese-chess/boardState.ts 中实现了标准棋盘初始化（9x10）、棋子位置设置、当前回合管理和游戏状态管理函数，包括棋子移动、坐标校验和游戏状态更新。
2026-04-10 20:09 | S3 | 实现车（Rook）的移动规则验证 | PASS | 在 gomoku-web/src/chinese-chess/rules/rook.ts 中实现了车的移动规则：直线移动，不能越过其他棋子。
2026-04-10 20:12 | S4 | 实现马（Knight）的移动规则验证 | PASS | 在 gomoku-web/src/chinese-chess/rules/knight.ts 中实现了马的移动规则：日字移动，检查绊马腿（马腿位置是否有棋子）。
2026-04-10 20:13 | S5 | 实现炮（Cannon）的移动规则验证 | PASS | 在 gomoku-web/src/chinese-chess/rules/cannon.ts 中实现了炮的移动规则：直线移动，吃子时必须隔一个棋子（炮架）。
2026-04-10 20:14 | S6 | 实现兵（Pawn）的移动规则验证 | PASS | 在 gomoku-web/src/chinese-chess/rules/pawn.ts 中实现了兵的移动规则：过河前只能向前，过河后可左右移动，不能后退。
2026-04-10 20:16 | S7 | 实现士（Advisor）和象（Elephant）的移动规则验证 | PASS | 在 gomoku-web/src/chinese-chess/rules/advisor-elephant.ts 中实现了士和象的移动规则：士斜线移动（九宫格内）；象田字移动，不能过河，塞象眼。
2026-04-10 20:18 | S8 | 实现将（King）的移动规则验证 | PASS | 在 gomoku-web/src/chinese-chess/rules/king.ts 中实现了将的移动规则：九宫格内移动，将帅不能直接照面（中间无棋子）。
2026-04-10 20:23 | S9 | 实现将军检测逻辑 | PASS | 在 gomoku-web/src/chinese-chess/rules/check.ts 中实现了将军检测：检查当前玩家是否被将军（将/帅受到攻击）。
2026-04-10 20:30 | S10 | 实现将死检测逻辑 | PASS | 在 gomoku-web/src/chinese-chess/rules/checkmate.ts 中实现了将死检测：生成所有合法移动，检查是否被将军且无逃脱移动，同时检测困毙。
2026-04-10 21:00 | S11 | 创建游戏逻辑主文件 | PASS | 在 gomoku-web/src/chinese-chess/gameLogic.ts 中集成所有规则，导出主要函数（移动验证、将军检测、胜负判定等）。
2026-04-10 21:10 | S12 | 编写单元测试框架配置 | PASS | 在 gomoku-web/vitest.config.ts 中配置了 Vitest 测试环境，支持 TypeScript 和 Vue 组件测试。
2026-04-10 21:10 | S13 | 为每种棋子规则编写单元测试 | PASS | 已为车、马、炮、兵、士、象、将编写完整的单元测试，修复了将左移测试的棋盘配置，所有测试通过。
2026-04-10 21:35 | S14 | 为将军和将死检测编写单元测试 | PASS | 修复了原有的错误测试用例，调整了将军、将死和困毙的测试场景，确保所有单元测试通过。同时修复了 generateAllLegalMoves 函数，使其过滤掉导致己方被将军的移动。
2026-04-10 22:21 | S15 | 创建中国象棋 AI 基础框架 | PASS | 在 gomoku-web/src/chinese-chess/ai/minimax.ts 中实现了基本的 minimax 搜索算法，包含棋子价值评估、位置价值表、Alpha-Beta 剪枝，支持不同难度级别。
2026-04-10 22:25 | S16 | 将 AI 算法迁移到 Web Worker | PASS | 在 gomoku-web/src/chinese-chess/ai/aiWorker.ts 中创建了 Web Worker，接收棋盘状态、AI 玩家方和难度级别，调用 minimax 算法返回最佳移动。
2026-04-10 22:45 | S17 | 创建中国象棋棋盘 UI 组件 | PASS | 在 gomoku-web/src/components/chinese-chess/Board.vue 中实现了 9x10 棋盘 UI，支持棋子显示、点击交互、选中高亮、合法移动提示，使用 i18n 翻译和 Tailwind 语义化颜色。
2026-04-12 00:00 | S18 | 创建中国象棋游戏控制组件 | PASS | 在 gomoku-web/src/components/chinese-chess/GameControls.vue 中实现了游戏控制组件，包括模式选择、难度设置、AI执方选择、悔棋、重新开始、提示、显示步骤、保存棋谱等功能，复用现有 Gomoku 组件结构和样式，使用 i18n 翻译和 Tailwind 语义化颜色。
2026-04-12 11:20 | S19 | 创建中国象棋主视图 | PASS | 在 gomoku-web/src/views/ChineseChessView.vue 中创建了中国象棋主视图，整合棋盘、控制面板、状态显示，管理游戏状态，复用 GameView.vue 的结构，添加了必要的 i18n 翻译键。
2026-04-12 19:32 | S20 | 在 i18n 中添加中国象棋翻译键 | PASS | 在 i18n.ts 中添加了缺失的翻译键，确保所有语言键一致，并删除了调试日志。
2026-04-12 20:00 | S21 | 集成到现有路由和导航 | PASS | 在 gomoku-web/src/router/index.ts 中添加中国象棋路由，更新导航菜单，添加 i18n 翻译键。
2026-04-12 20:30 | S22 | 进行端到端测试和 UI 验证 | PASS | 完成所有端到端测试：类型检查、单元测试、AI 对战验证、规则正确性验证、UI 集成与 i18n 翻译检查，所有测试通过。
2026-04-12 20:50 | S20a | 修复棋子字符硬编码 | PASS | 将棋子字符从 Board.vue 移至 i18n 翻译，新增 14 个翻译键，确保符合国际化规范。
2026-04-12 20:45 | S23a | 实现落子合法路径计算逻辑 | PASS | 在 engine.ts 中实现了 getLegalMovesForPiece 方法，能够根据棋子位置返回所有合法落子坐标，并包含单元测试验证。
2026-04-12 20:50 | S23b | 渲染带呼吸灯效果的落子提示 | PASS | 在 Board.vue 中添加了呼吸动画效果，合法落子提示点透明度在 0.3 到 0.8 之间循环，周期 1.5 秒。
2026-04-12 13:00 | S24 | 重构象棋传统坐标系显示 | PASS | 修改棋盘边缘渲染，上方（黑方）显示数字 1-9，下方（红方）显示中文数字‘九’至‘一’。使用 i18n 翻译键 columnTopNumbers 和 columnBottomChineseNumbers，确保与棋盘格子精准对齐。
2026-04-12 21:01 | S25a | 完善悔棋底层数据结构 | PASS | 修改 MoveHistory 接口以包含被吃掉的棋子的类型和颜色（CapturedPiece），更新 boardState.ts 和 gameLogic.ts 中的相关函数，确保移动记录包含完整的被吃掉的棋子信息。
2026-04-12 21:01 | S25b | 修复悔棋 UI 复原逻辑 | PASS | 根据 S25a 完善的数据，更新 ChineseChessView.vue 中的悔棋逻辑，在撤销移动时将被吃掉的棋子重新渲染到棋盘上。

2026-04-12 21:15 | S26a | 实现中国象棋标准记谱法逻辑 | PASS | 开发专门的记谱器（Notation Generator），将坐标移动转换为‘炮二平五’、‘马八进七’等标准中文术语。处理红黑方差异。
2026-04-12 21:15 | S26b | 实时渲染对局棋谱列表 | PASS | 在右侧面板展示记谱术语列表，支持自动滚动至底部，样式与五子棋对战记录保持一致。
2026-04-12 21:30 | S27 | 移除调试日志语句 | PASS | 移除所有 console.log 语句以符合生产代码规范，清理了 GameView.vue、GomokuNN.ts、game-api.ts 中的调试输出。
2026-04-12 21:45 | S27a | 全局品牌更名：棋林（QiLin） | PASS | 在前端 i18n、HTML Title 和 Footer 中，将所有'五林'或'五子棋'品牌文案更改为'棋林'。更新了 i18n.ts 中的 agentTitle、agentGreeting、agentDisclaimer、agentResponse、loginSubtitleLogin、navAgent 等翻译键，将'五林'改为'棋林'，英文'Wulin'改为'Qilin'，日文'五林エージェント'改为'棋林エージェント'。同时更新了 App.vue 和 LoginView.vue 中的标题，以及 index.html 的页面标题。
2026-04-12 21:45 | S27b | 导航栏与路由标签优化 | PASS | 在前端组件中将'去下棋'标签重命名为'五子棋'，更新了 i18n.ts 中的 navGame 翻译键（简体中文、繁体中文、英文、日文）。新增的'中国象棋'标签已存在。确保路由配置文件中的面包屑和页面标题已更新。
2026-04-12 21:58 | S28 | 棋林智能体（Agent）提示词升级 | PASS | 更新后端聊天服务的系统提示词，从五子棋专用改为棋林通用棋类专家。更新前端 i18n 翻译键，使智能体问候语和占位符包含五子棋和中国象棋。
2026-04-12 22:16 | S29 | 前端目录模块化：提取共享逻辑 | PASS | 在 src 目录下创建 common 文件夹，移动 SettingsModal、AnswerContent、MessageActions、ThinkingProcess 四个通用 UI 组件到 common/components/ui/，更新 App.vue 和 AgentView.vue 中的导入路径，删除原始文件。
2026-04-13 18:57 | S30a1 | 前端：五子棋逻辑与状态层迁移 | PASS | 在 src/games/gomoku/ 下建立基础结构，移动五子棋专属的类型定义、AI Web Worker、状态管理文件（gameLogic.ts、aiWorker.ts、openingBook.ts、nn/GomokuNN.ts），更新相关导入路径，确保类型检查通过。

[S30a2] - 前端：五子棋 UI 组件层迁移 - 2026-04-13 - Completed
[S30a3] - 前端：五子棋视图与全局路由修复 - 2026-04-13 - Completed
[S30b1] - 前端：中国象棋逻辑与状态层迁移 - 2026-04-13 - Completed (by previous work)
[S30b2] - 前端：中国象棋 UI 组件与视图迁移 - 2026-04-13 - Completed
[S31] - 后端 Express：创建模块化路由结构 - 2026-04-13 - Completed
[S32] - 后端：五子棋业务逻辑迁移 - 2026-04-13 - Completed
[S33] - 后端：中国象棋业务逻辑迁移 - 2026-04-13 - Completed
[S34] - 后端：重构主入口文件（app.ts/index.ts） - 2026-04-13 - Completed[S35] - 全链路冒烟测试与重构验证 - 2026-04-13 - Completed
[Bugfix-1] - 修复 Board.vue 渲染崩溃与完善选中交互 - 2026-04-13 - Completed
[Bugfix-2] - 修复并实现右侧对战记录（棋谱）渲染 - 2026-04-13 - Completed

[Bugfix-3] - 修复吃子目标格无提示灯的 Bug - 2026-04-13 - Completed
[Feature-1a] - 实现特殊对局状态的检测与事件派发 - 2026-04-13 - Completed[Feature-Theme-1] - 定义全局主题配置与常量 - 2026-04-14 - Completed
[Feature-Theme-2] - 实现独立的主题状态管理 - 2026-04-14 - Completed
[Feature-Theme-3] - 开发公共的主题切换选择器 - 2026-04-14 - Completed
[Feature-Theme-4] - 将主题应用到五子棋视图 - 2026-04-14 - CompletedFeature-Theme-5: Applied theme system to Chinese Chess board components (Board.vue). Replaced hardcoded amber colors with dynamic theme colors from theme.ts. Updated piece colors, board background, lines, river styles, and dynamic elements (thinking path, hover indicators) to use theme colors. Ensured smooth transitions and preserved breathing dots and capture hints.
Bugfix-Theme-Render: Fixed frontend white‑screen crash caused by theme configuration. Resolved prop type mismatch (Object vs String) by unwrapping the ref `settings.chessTheme` in ChineseChessView.vue. Added defensive fallback in `getThemeColors()` to prevent undefined access. Board UI now renders correctly with dynamic theme colors.Ralph Agent: Verified Chinese Chess implementation integrity, ran quality checks, all tests passed - 2026年04月15日 18:31:16
UI-Refine-1: Removed alternating square background logic (chessboard pattern) from Board.vue. Now all cells use uniform boardBackground color. River boundaries remain correctly styled with dashed borders and no fill anomalies. - 2026年04月15日 18:31:16
UI-Refine-2: Updated piece styling with solid opaque background (pieceBackground), border, and shadow. Red/black text colors now properly defined per theme with cyber theme getting fluorescent colors and drop-shadow effects. - 2026年04月15日 18:31:16
UI-Refine-3: Rewrote theme.ts with exact color specifications per requirements. All four themes (default, zen, cyber, minimal) now use precise hex colors for board, lines, piece background, and text colors. - 2026年04月15日 18:31:16
UI-Fix-Piece-Color, UI-Fix-Grid-Lines: Updated theme.ts to fix piece text colors (removed dark: variants, forced red/black colors) and increased grid line contrast per theme. - 2026年04月15日 19:51:34
Ralph Agent: Verified all stories completed, project ready for new stories. - 2026年04月15日 20:00:00
UI-Fix-Piece-Contrast, UI-Fix-Board-Boundary: Fixed piece text color contrast (removed dark variants, forced red/black colors) and improved board boundary clarity (removed shadows, increased grid line thickness). - 2026年04月15日 20:17:25
Quality Checks: Passed type checking, linting, tests, and build. All 176 tests passed, build successful. - 2026年04月15日 20:30:00
UI-Opt-Board-Layout: Removed coordinate labels (top, bottom, left) and enlarged board grid and piece sizes (cells w-8->w-12, pieces 90%, text xl/2xl). Passed type check, tests, build. - 2026年04月16日 21:01:03
UI-Opt-River-Style: Refactored Chu River/Han Boundary styling - removed duplicate text and dashed borders, merged river area, positioned '楚河' left and '汉界' right with enlarged font matching line color. Passed type check, tests, build. - 2026年04月16日 21:59:59
UI-Fix-Piece-Text-Color: Removed hardcoded HEX colors from piece text styling, replaced with semantic theme classes (pieceTextPrimary/pieceTextSecondary). Passed type check, tests, build. - 2026年04月16日 22:16:00
Refactor-Any-Types: Replaced any types in notation.ts with proper BoardState types. Passed type check, tests, build. - 2026年04月16日 22:20:00
UI-Refactor-Board-Grid: Refactored board grid to use SVG background layer with separated piece layer. Implemented 9 vertical lines (broken at river), 10 horizontal lines, palace diagonals using inline SVG. Removed nested div lines, ensured clean architecture. Passed type check, tests, build. - 2026年04月17日 18:52:00
UI-SVG-Math-Blueprint: Injected precise SVG coordinate system (viewBox 0 0 900 1000) with 50px padding. Added outer border rect, 8 internal horizontal lines, 7 internal vertical lines broken at river, palace diagonals. All lines use stroke color from theme lineColor and stroke-width=2. Passed type check and build. - 2026年04月17日 19:00:00
UI-Tweak-River-Text: 调整楚河汉界文字垂直居中，使用SVG text元素，字体放大加粗，颜色与线条色一致。通过类型检查和测试。 - 2026年04月17日 20:38:00
Logic-Unified-Record-Service: 构建通用棋谱数据服务层。1. Prisma Game模型新增gameType字段(gomoku/chinese_chess)，运行migration。2. 后端types新增GameType类型，Game/GameCreateInput/GameUpdateInput/GameQueryParams均包含gameType。3. GameService.createGame强制Admin为isPublic:true，普通用户为isPublic:false；getGames/getUserGames支持gameType过滤。4. GomokuService/Controller自动注入gameType='gomoku'并按类型过滤。5. ChineseChessController实现完整CRUD(创建/列表/详情/删除)，自动注入gameType='chinese_chess'。6. 添加中国象棋路由(/api/games/chinese-chess/frontend)，使用optionalAuthenticate。7. 客户端API新增GameType/GameListItem类型，saveGame/getGames/getGame/deleteGame按gameType路由到对应后端端点。8. 游戏转换器更新支持gameType。Server typecheck通过，web lint通过。 - 2026年04月20日
UI-Refactor-Gomoku-Records: 基于Unified-Record-Service重构五子棋棋谱列表。1. 移除SavedGame类型，改用GameListItem直接渲染列表。2. 提取fetchRecords('gomoku')函数替代onMounted中的filter/mapping代码。3. 移除所有localStorage回退逻辑和默认棋谱创建代码。4. 移除所有console.error调试日志，改用用户通知。5. 保存/编辑/删除操作统一使用GAME_TYPE='gomoku'参数调用API。6. 棋谱列表渲染公开/私有徽章标签(基于is_public)，新增recordAuthor显示作者。7. 修复后端路由：routes/index.ts改用games/index.ts统一挂载，使前端API路径/api/games/gomoku/frontend正确匹配。8. 运行Prisma迁移添加gameType字段到数据库。9. 新增i18n翻译键(saveFailed/importFailed/updateFailed/deleteFailed/recordAuthor)。通过类型检查、176项单元测试、浏览器截图验证。 - 2026年04月21日
UI-Implement-Chess-Records: 重构中国象棋棋谱列表功能，复刻五子棋交互模式。1. 新增GAME_TYPE='chinese_chess'常量，提取fetchRecords函数统一调用gameApi.getGames(GAME_TYPE)。2. 移除SavedGame类型，改用GameListItem直接渲染列表。3. 移除所有localStorage回退逻辑和console.error调试日志。4. 保存/编辑/删除操作统一使用GAME_TYPE参数调用API。5. 棋谱列表渲染公开/私有徽章标签(基于isPublic)，显示作者信息。6. 更新game-api.ts新增ChineseChessFrontendGame类型。7. 权限逻辑(Admin公有/个人私有)在象棋中同样生效。通过TypeScript类型检查、单元测试、生产构建验证。 - 2026年04月21日
Bugfix-Chess-Board-Replay: 修复象棋导入棋谱后棋盘状态未重建导致视图消失的问题。1. 问题根因：后端chinese-chess.controller.ts的getGameForFrontend方法返回board:[]空数组，前端importGame函数直接使用该空数组或仅初始化棋盘，未执行步骤推演(Replay)重建最终棋盘状态。2. 修复方案：在importGame中，先初始化棋盘为开局状态，然后遍历parsedMoveHistory执行每一步移动，重建最终棋盘状态后赋值给响应式变量board.value。3. 代码变更：ChineseChessView.vue的importGame函数，移除对fullGame.board的直接依赖，改用replayBoard本地变量推演后赋值。通过TypeScript编译、单元测试验证。 - 2026年04月21日
Bugfix-Chess-Import-Crash: 修复中国象棋导入棋谱导致页面崩溃的问题。1. 后端chinese-chess.controller.ts：修复棋谱序列化逻辑，不再将五子棋的Move格式强加于象棋，改用类型断言处理ChineseChessMove[]结构。2. 新增ChineseChessMove类型定义，正确处理象棋的move对象（from, to, piece, side, timestamp, capturedPiece）。3. 前端game-api.ts：修复getGame返回类型，支持ChineseChessFrontendGame。4. 前端ChineseChessView.vue：增强importGame函数健壮性，优雅处理旧格式数据而非崩溃，添加空值检查和类型过滤。通过TypeScript类型检查、API端点测试验证。 - 2026年04月21日
Bugfix-Record-Permission-Leak: 修复Admin越权查看他人私有棋谱的严重权限Bug。1. 问题根因：三个控制器(game.controller.ts, gomoku.controller.ts, chinese-chess.controller.ts)的getGamesForFrontend方法使用错误的权限逻辑，任何登录用户都能看到自己的私有棋谱，但该逻辑绕过了Admin的权限限制。2. 修复方案：重构权限过滤逻辑，严格遵循真值表：Admin只能看到公开棋谱(isPublic:true)；非Admin能看到公开棋谱+自己的私有棋谱；未登录只能看到公开棋谱。3. 代码变更：新增userRole检查，将条件从if(userId)改为if(userId && userRole !== 'ADMIN')，确保Admin不会意外获取他人私有记录。通过TypeScript编译、前后端构建验证。 - 2026年04月21日
Bugfix-Chess-Board-Replay-Verification: 验证象棋棋盘重建修复。1. 创建独立测试脚本模拟importGame逻辑，验证后端返回空board数组时，前端能正确从moveHistory重建棋盘状态。2. 测试覆盖：初始棋盘32子(红黑各16)、移动后棋子位置正确、旧位置清空、吃子逻辑正确。3. 所有测试通过，确认修复有效。通过Node.js单元测试验证。 - 2026年04月21日
LLM-001-Types: 创建LLM AI服务类型定义。1. 在 `gomoku-server/src/types/llm.types.ts` 定义完整类型系统：`BoardCell`/`BoardState` 棋盘状态类型、`PlayerColor` 玩家颜色、`MoveHistoryEntry` 历史记录、`LLMMoveRequest` 请求接口(board/currentPlayer/history)、`LLMMoveResponse` 响应接口(move/reason)、`ValidatedMove` 校验结果、`LLMConfig` API配置、`LLMServiceError` 错误类型。2. 在 `types/index.ts` 导出所有类型。3. TypeScript编译通过。 - 2026年04月25日
LLM-002-Prompt-Builder: 创建棋盘转换与Prompt组装工具。1. 在 `gomoku-server/src/utils/boardPromptUtils.ts` 实现完整工具集：`numericToAsciiBoard`/`asciiToNumericBoard` 数值与ASCII棋盘互转、`boardToAsciiString` 生成带行列标注的ASCII可视化、`coordinatesToNotation`/`notationToCoordinates` 代数记号转换(如H8)、`formatMoveHistory` 格式化历史记录、`buildSystemPrompt` 组装完整System Prompt(含棋盘状态、当前玩家、历史记录、思维链指引)、`buildUserPrompt` 用户提示、`createLLMRequest` 从前端状态创建请求对象、`getEmptyPositions`/`getRandomEmptyPosition` 空位查询与随机选择(兜底策略)。2. TypeScript编译通过。 - 2026年04月25日
LLM-003-API-Service: 实现DeepSeek API调用与幻觉校验层。1. 在 `gomoku-server/src/services/llm-ai.service.ts` 实现核心服务：`validateMove` 函数校验坐标格式与空位检查、`parseLLMResponse` 解析LLM JSON响应(支持多种格式)、`getFallbackMove` 幻觉兜底策略(随机空位)、`LLMAIService` 类封装DeepSeek API调用(fetch)、`generateMove` 主方法生成落子、`generateMoveFromFrontend` 前端格式便捷方法。2. 在 `gomoku-server/src/controllers/llm-ai.controller.ts` 实现控制器：`generateMove` POST接口处理(含完整参数校验)、`healthCheck` 健康检查(检测API Key配置)。3. 在 `gomoku-server/src/routes/games/gomoku.routes.ts` 添加路由：`GET /llm/health`、`POST /llm/move`。4. TypeScript编译通过。 - 2026年04月25日
LLM-004-Frontend-API: 前端API调用与状态管理。1. 在 `gomoku-web/src/games/gomoku/api/llmApi.ts` 创建LLM API服务：`LLMPlayerColor` 类型定义(black/white)、`LLMMoveRequest` 请求接口(board/currentPlayer/moveHistory)、`LLMMoveResponse` 响应接口(x/y/reason/isFallback)、`LLMHealthResponse` 健康检查接口、`LLMApiService` 类封装fetch请求、`healthCheck` 方法检查服务状态、`generateMove` 方法生成落子。2. 在 `gomoku-web/src/games/gomoku/views/GameView.vue` 修改AI落子逻辑：导入 `llmApi` 服务、新增 `llmMove` 异步函数调用LLM API、修改 `aiMove` 函数在 `difficulty === 'neural'` 时调用 `llmMove`、复用现有 `isAiThinking` 状态。3. 在 `gomoku-web/src/i18n.ts` 添加 `llmMoveFailed` 翻译键(四语言)。4. TypeScript编译通过。 - 2026年04月25日
LLM-005-UI-Integration: 前端UI交互集成与加载反馈。1. 验证难度下拉菜单已有 '神经网络(DeepSeek)' 选项(GameControls.vue line 111)。2. 验证棋盘点击已在AI回合时禁用(GameView.vue `placePiece` line 520)。3. 在 `GameView.vue` 添加LLM思考提示：当 `isAiThinking && aiDifficulty === 'neural'` 时显示 'DeepSeek正在推演局势...' 加载提示(lines 681-685)，使用animate-pulse动画、indigo配色、支持深浅主题。4. 在 `i18n.ts` 添加 `llmThinking` 翻译键(四语言)。5. TypeScript编译通过。 - 2026年04月25日
LLM-Opt-001-Threat-Detector: 威胁检测扫描器实现。1. 在 `gomoku-server/src/utils/threatDetector.ts` 创建威胁检测模块：`findCriticalMove(board, aiColor)` 主函数扫描15x15棋盘、`checkWinningMove` 检测AI必胜点(五连)、`checkRushFour` 检测对手冲四威胁(必须堵截)、`checkLiveThree` 检测对手活三威胁(必须堵截)、`findCriticalMoveFromNumeric` 便捷方法支持数字棋盘格式。2. 优先级排序：必胜(100) > 冲四(90) > 活三(80)。3. TypeScript编译通过。 - 2026年04月25日
LLM-Opt-002-Service-Interceptor: LLM服务层威胁拦截器集成。1. 在 `llm-ai.service.ts` 的 `generateMove` 方法中集成威胁检测：调用大模型前先执行 `findCriticalMove(board, currentPlayer)`。2. 若检测到关键坐标，直接返回本地响应，跳过DeepSeek API调用，响应格式 `{ move, reason, isFallback: true }`。3. 原因文案映射：必胜点/冲四堵截/活三堵截 三种场景。4. 只有当威胁检测返回null时才调用大模型API。5. TypeScript编译通过。 - 2026年04月25日
LLM-OPT-01-Advanced-Threats: 重构威胁检测器实现冲四与活三防御。1. 重构 `threatDetector.ts`：移除原有错误的 `checkLiveThree` 和 `checkRushFour` 函数，改用正确的模式检测逻辑。2. 新增 `detectOpponentLiveThree` 函数：检测对手活三(.OOO.)和间隙活三(.O.OO./.OO.O.)，返回堵截坐标。3. 新增 `detectOpponentRushFour` 函数：检测对手冲四(XOOOO./.OOOOX)、开放四(.OOOO.)、间隙四(O.OOO/OO.OO/OOO.O)，返回堵截坐标。4. 新增 `getLinePattern` 辅助函数：提取指定方向的棋子序列用于模式匹配。5. 优先级排序：必胜(100) > 冲四堵截(95) > 活三堵截(90)。6. 新增 `threatDetector.test.ts`：14个单元测试覆盖必胜检测、冲四堵截、活三堵截、优先级排序、数值棋盘格式转换。7. 新增 `jest.config.js`：配置ts-jest支持TypeScript测试。8. TypeScript编译通过，所有测试通过。 - 2026年04月25日
LLM-OPT-02-Candidate-Generator: 实现启发式候选落子生成器。1. 新建 `candidateGenerator.ts`：实现棋型评估算法，遍历棋盘所有空位计算攻防分值。2. 棋型定义：连五(100000) > 活四(10000) > 冲四(1000) > 活三(500) > 眠三(100) > 活二(50) > 眠二(10)。3. 核心函数：`generateCandidateMoves(board, aiColor, topN)` 返回Top N候选点数组，包含坐标、攻防分值、检测到的棋型列表、落子理由。4. 攻防评分：攻击分值按棋型基础分计算，防守分值按基础分*0.9计算（优先进攻）。5. 落子理由生成：根据检测到的棋型自动生成中文描述（如"必胜点：连五获胜"、"进攻点：构成活三"、"防守点：堵截对手活四"）。6. 辅助函数：`generateCandidateMovesFromNumeric` 支持数值棋盘格式(0/1/2)，`getBestFallbackMove` 返回最高分候选点用于兜底。7. 新增 `candidateGenerator.test.ts`：25个单元测试覆盖连五检测、活四/冲四/活三检测、防守检测、评分计算、多方向棋型、数值棋盘转换。8. TypeScript编译通过，所有测试通过。 - 2026年04月25日
LLM-OPT-03-Advanced-Prompt-Engineering: 重构System Prompt注入专业五子棋知识体系。1. 重构 `boardPromptUtils.ts` 的 `buildSystemPrompt` 函数，新增 `candidates` 可选参数接收候选落子列表。2. 注入专业术语定义表：连五 > 活四 > 冲四 > 活三 > 眠三 > 活二，含中英文名称、描述和威胁等级。3. 设定策略优先级：必胜检测 > 必防检测 > 进攻机会 > 防守需求 > 布局发展，核心原则为进攻优于防守但优先处理必杀威胁。4. 强制CoT思维链：要求AI在JSON响应中必须输出 `situation_analysis` 字段，包含 `my_threats`、`opponent_threats`、`strategy` 三个子字段，先分析局势再决定落子。5. 动态接入Top 10候选名单：将 `candidateGenerator` 生成的候选点注入Prompt，要求AI必须从候选列表中选择并给出 `candidate_index` 和选择理由。6. 更新 `llm.types.ts`：新增 `SituationAnalysis` 接口，扩展 `LLMMoveResponse` 增加 `situation_analysis` 和 `candidate_index` 可选字段。7. 更新 `llm-ai.service.ts`：`generateMove` 方法集成候选生成器，调用大模型前先生成Top 10候选；LLM响应必须从候选列表中选择，否则使用最高分候选兜底；返回类型增加 `situation_analysis` 字段。8. TypeScript编译通过，所有测试通过。 - 2026年04月25日
LLM-OPT-04-Integration-Smart-Fallback: 集成服务层与智能兜底逻辑。1. 验证 `llm-ai.service.ts` 已完整实现三层防护架构：第一层 `findCriticalMove` 威胁拦截器（检测必胜点和必防点）、第二层 `generateCandidateMoves` 候选生成器（启发式评估Top 10候选）、第三层 LLM API 调用（带候选列表的高级Prompt）。2. 智能兜底机制：LLM返回非法JSON时使用最高分候选兜底；LLM返回已占用坐标时使用最高分候选兜底；LLM选择非候选位置时使用最高分候选兜底；API调用失败时使用最高分候选兜底。3. 新增 `llm-ai.service.test.ts`：14个集成测试覆盖威胁拦截（必胜/冲四/活三）、候选兜底（非法JSON/占用坐标/非候选位置/API错误）、成功LLM响应、边界条件（空棋盘/满棋盘）。4. TypeScript编译通过，53个测试全部通过。 - 2026年04月25日
SPLIT-01-Global-State: 创建分屏对弈的全局状态管理。1. 新建 `gomoku-web/src/composables/useAgentPlay.ts`。2. 定义 `PlayMode` 类型：'chat' | 'gomoku'。3. 响应式状态：`playMode` 默认 'chat'，`isAIThinking` 默认 false。4. 状态切换方法：`enterGomokuMode()` 进入对弈模式，`exitPlayMode()` 退出并重置状态。5. 导出全局单例 `useGlobalAgentPlay()` 确保跨组件状态共享。6. TypeScript编译通过。 - 2026年04月25日
SPLIT-02-Chat-Message-UI: 实现可折叠的AI思考UI。1. 在 `AgentView.vue` 的 `Message` 接口中新增 `reasoningContent?: string` 字段。2. 修改三个流式响应完成回调（sendMessage、regenerateStreamingAnswer、regenerateAnswer），将 `thinkingContent` 持久化到消息历史的 `reasoningContent` 字段（之前流式结束后思考内容被丢弃）。3. 在历史AI消息气泡中，当 `msg.reasoningContent` 存在时，在主体文本上方渲染 `<details>/<summary>` 折叠区域：summary标题使用i18n key `agentReasoningSummary`（中文：'🧠 Agent 正在推演局势...'），默认折叠；内部文本使用 `text-xs font-mono opacity-70` 调小置灰。4. 添加 `<details>` 箭头旋转动画（open时旋转90度）。5. 新增i18n key `agentReasoningSummary` 到 zh-CN.json 和 en-US.json，以及 i18n.ts 的 Messages 接口。6. TypeScript编译通过。 - 2026年04月25日
SPLIT-03-Home-Action-Chips: Agent首页添加对弈快捷指令入口。1. 在 `AgentView.vue` 中引入 `useGlobalAgentPlay` 状态管理。2. 在欢迎语下方添加两个胶囊按钮：'💬 棋林对话'（次要样式）和 '⚔️ 棋林对弈'（主色indigo-600）。3. 点击'⚔️ 棋林对弈'时调用 `enterGomokuMode()` 并插入系统提示消息（i18n key `agentGomokuModeEntered`）。4. 新增i18n keys: `agentActionChat`、`agentActionGomoku`、`agentGomokuModeEntered`。5. TypeScript编译通过。 - 2026年04月25日
SPLIT-04-Play-Panel-Wrapper: 创建右侧对弈面板包装组件。1. 在 `gomoku-web/src/components/` 下新建 `AgentGomokuPanel.vue`。2. 组件内部引入 `Board.vue` 并居中渲染，顶部包含撤销和认输按钮。3. 组件维护独立的棋盘状态（board、currentPlayer、winner、moveHistory等），用户执黑，AI执白。4. 用户落子时通过 `emit('userMove', r, c)` 向父组件抛出事件。5. 通过 `defineExpose` 暴露 `placeAiPiece`、`resetGame`、`getBoard`、`getMoveHistory` 方法供父组件调用。6. 新增i18n key `agentSurrender` 到 zh-CN.json、en-US.json 和 Messages 接口。7. TypeScript编译通过（组件本身无错误）。 - 2026年04月25日
SPLIT-05-Split-Screen-Layout: Agent视图实现左右分屏响应式布局。1. 修改 `AgentView.vue`，引入 `useGlobalAgentPlay` 的 `playMode` 和 `exitPlayMode` 状态/方法。2. 根容器根据 `playMode` 动态切换：chat模式保持单列居中布局，gomoku模式切换为 `flex row` 左右分屏。3. 左侧聊天区在分屏模式下宽度35%（min-w:320px, max-w:500px），右侧引入 `AgentGomokuPanel.vue` 占据剩余空间。4. 分屏模式顶部添加"返回对话"按钮（ArrowLeft图标），调用 `exitPlayMode()`。5. 添加 `handleUserMove` 事件处理（将落子坐标转为H8格式并插入系统消息）和 `handleSurrender` 事件处理。6. 新增i18n keys: `agentBackToChat`、`agentUserMoveNotification`、`agentSurrenderNotification`。7. 切换时使用 `transition-all duration-300 ease-in-out` 实现平滑过渡。8. TypeScript编译通过。 - 2026年04月25日
SPLIT-06-Interaction-User-Move: 数据桥接：拦截落子触发 Agent 思考。1. 创建 `gomoku-web/src/api/gomoku-ai-api.ts` 服务文件，封装 LLM AI 落子生成接口 `/api/games/gomoku/llm/move`。2. 定义 `GomokuAiMoveRequest` 和 `GomokuAiMoveResponse` 接口类型。3. 修改 `AgentView.vue` 的 `handleUserMove` 方法：接收用户落子坐标后，向聊天流插入系统消息，设置 `isThinking` 状态为 true，调用 `gomokuAiApi.generateMove()` 发起 LLM 请求。4. 成功响应后，将 AI 思考内容插入聊天流，调用 `gomokuPanelRef.placeAiPiece(y, x)` 在棋盘上渲染白子。5. 错误处理：捕获异常并显示错误消息。6. 新增 i18n key `agentAiThinkingMove` 到 zh-CN.json、en-US.json 和 Messages 接口。7. TypeScript 编译通过。 - 2026年04月25日
SPLIT-07-Interaction-AI-Response: 数据桥接：解析 AI 响应并同步渲染。此任务已在 SPLIT-06 中一并完成。验证要点：1. AI 响应成功时，构造包含 `reasoningContent` 的消息插入聊天流（触发折叠 UI）。2. `isFallback` 为 true 时隐藏思考过程面板。3. 调用 `gomokuPanelRef.placeAiPiece(y, x)` 渲染白子。4. 关闭 `isThinking` 状态。5. 错误处理：捕获异常并显示带前缀的错误消息。6. TypeScript 编译通过。 - 2026年04月25日
UI-REFINE-01-Relocate-Action-Chips: 重构首页快捷指令入口位置。1. 删除欢迎语下方的 `agentActionChat` 按钮。2. 将 `agentActionGomoku` 按钮移动到底部聊天输入框正上方。3. 使用 `flex flex-wrap gap-3 mb-4` 布局预留横向排列空间。4. 按钮仅在 `messages.length === 0` 时显示（空状态）。5. `handleEnterGomokuMode` 功能保持正常。6. TypeScript 编译通过，生产构建成功。 - 2026年04月25日
UI-REFINE-02-Draggable-Divider-State: 实现分屏拖拽的响应式状态与逻辑。1. 声明响应式变量 `leftPanelWidth`（默认 35%）。2. 编写拖拽状态：`isDragging`、`startX`、`startWidth`。3. 实现 `startDrag(event)` 方法：记录初始鼠标位置和宽度，绑定 `mousemove` 和 `mouseup` 到 `window`。4. 实现 `onDrag(event)` 方法：计算移动差值，动态更新 `leftPanelWidth`。5. 使用 `Math.max(25, Math.min(50, newWidth))` 强制宽度限制在 25%-50% 之间。6. 实现 `stopDrag()` 方法：移除全局事件监听。7. TypeScript 编译通过。 - 2026年04月25日
UI-REFINE-03-Draggable-Divider-Render: 渲染分割线组件与宽度动态绑定。1. 在分屏模式下，左侧聊天区和右侧棋盘区之间插入分割线 `div` 元素。2. 绑定 `@mousedown.prevent="startDrag"` 事件。3. 样式：宽度 `w-1`（4px），`cursor-col-resize`，Hover 效果变为品牌色 `bg-indigo-400`，拖拽中显示 `bg-indigo-500`，过渡动画 `transition-colors duration-200`。4. 左侧聊天区宽度动态绑定为 `:style="\`width: \${leftPanelWidth}%\`"`，右侧容器保持 `flex-1`。5. TypeScript 编译通过，生产构建成功。 - 2026年04月25日
BUGFIX-UI-Divider-Drag: 修复分屏拖拽失效与计算单位错误。1. 添加 `shrink-0` (flex-shrink: 0) 防止左侧聊天面板被右侧棋盘面板挤压。2. 添加 `z-index: 50` 防止分割线被其他元素遮挡。3. 在 `startDrag` 时设置 `document.body.style.userSelect = 'none'` 防止拖拽时意外选中文字。4. 在 `stopDrag` 时清除 `userSelect` 样式。5. 边界限制（25%~50%）保持不变。6. TypeScript 编译通过，生产构建成功。 - 2026年04月25日
BUGFIX-UI-Drag-CSS-Conflicts: 修复分割线拖拽的 CSS 样式冲突。1. 移除左侧聊天容器的 `max-w-[500px]` 限制，保留 `min-w-[320px]`，让 width 百分比真正生效。2. 将 `transition-all duration-300` 改为动态绑定，仅当 `!isDragging` 时才应用过渡动画，避免拖拽时的视觉延迟。3. TypeScript 编译通过，生产构建成功。 - 2026年04月25日
FLOW-REFINE-01-Types-And-Trigger: 修改消息数据结构与对弈触发逻辑。1. 在 `AgentMessage` 接口中增加 `isGameSelector?: boolean` 属性。2. 在 i18n 文件中新增 `agentGameSelectorPrompt`、`agentGameGomoku`、`agentGameChineseChess` 翻译键。3. 修改 `handleEnterGomokuMode` 方法：删除 `enterGomokuMode()` 调用，改为推送带 `isGameSelector: true` 的消息。4. TypeScript 编译通过，生产构建成功。 - 2026年04月25日
FLOW-REFINE-02-Render-Buttons: 在聊天消息组件中渲染游戏选择面板。1. 在 `AgentChatMessages.vue` 的 `defineEmits` 中新增 `selectGame` 事件。2. 在 AI 消息气泡中添加游戏选择按钮区块，使用 `v-if="msg.isGameSelector"` 条件渲染。3. 五子棋按钮使用 indigo-600 高亮样式并绑定点击事件，中国象棋按钮置灰禁用。4. 使用语义化 Tailwind 类名，支持深色模式。5. TypeScript 编译通过，生产构建成功。 - 2026年04月25日
FLOW-REFINE-03-Handle-Selection: AgentView 实现选择回调与分屏流转。1. 在 `AgentChatMessages` 组件上添加 `@select-game="handleGameSelection"` 事件监听。2. 实现 `handleGameSelection(gameType, msg)` 方法：设置 `msg.isGameSelector = false` 隐藏按钮；若选择五子棋则调用 `enterGomokuMode()` 触发分屏；推送 `agentGomokuModeEntered` 提示消息；通过 `nextTick` 调用 `scrollToBottom()`。3. 导入 `AgentMessage` 类型和 `nextTick`。4. TypeScript 编译通过。 - 2026年04月25日
CHAT-MOVE-01-Regex-Helper: 编写坐标解析工具函数。1. 在 `gomoku-web/src/games/gomoku/` 目录下新建 `moveParser.ts`。2. 实现 `parseMoveText(text: string)` 函数：使用正则表达式匹配 'H8', 'h8', '落子 h8', '下 H8' 等格式。3. 字母 A-O 映射为列索引 (A=0, O=14)，数字 1-15 映射为行索引 (r = 15 - number)。4. 返回 `{ r, c, coord }` 或 null。5. 添加单元测试覆盖各种边界情况，所有测试通过。 - 2026年04月26日
CHAT-MOVE-02-Expose-Board-API: 对弈面板暴露合法性校验与强制落子方法。1. 在 `AgentGomokuPanel.vue` 中新增 `isValidMove(r, c)` 方法：校验坐标边界、游戏是否结束、位置是否为空、是否为玩家回合、禁手规则。2. 新增 `placeUserPieceFromChat(r, c)` 方法：调用 `isValidMove` 校验后执行落子逻辑，更新棋盘状态，检查胜负/和棋，触发 `userMove` 事件。3. 通过 `defineExpose` 暴露两个方法供父组件调用。4. TypeScript 编译通过。 - 2026年04月26日
CHAT-MOVE-03-Message-Interceptor: AgentView 聊天输入拦截与分流逻辑。1. 在 `AgentView.vue` 中导入 `parseMoveText` 函数。2. 修改 `handleSend` 方法：当 `playMode === 'gomoku'` 时，先解析用户输入。3. 若解析出坐标，调用 `isValidMove` 校验：合法则显示用户消息并执行落子，非法则提示用户"位置不合法或已有棋子"。4. 若未解析出坐标，走原有 `sendMessage` 逻辑。5. 新增 i18n 词条 `agentInvalidMove`。6. TypeScript 编译通过。 - 2026年04月26日
UI-SCROLL-01-Root-Constraint: 锁定对弈模式下的全局视口高度。1. 修改 `AgentView.vue` 根元素：移除静态 `h-full`。2. 动态绑定 `:class`：对弈模式 `h-screen overflow-hidden`，聊天模式 `min-h-screen`。3. TypeScript 编译通过。 - 2026年04月26日
UI-SCROLL-02-Flex-Layout-Fix: 实现左侧聊天列表内部独立滚动。1. 在 `AgentView.vue` 中为 `<AgentChatMessages>` 组件外层包裹新的 `<div>`。2. 包裹层绑定动态样式：对弈模式 `flex-1 overflow-y-auto`，聊天模式 `w-full`。3. 为 `<AgentChatInput>` 组件添加 `shrink-0` class 确保输入框固定在底部。4. 为 `<AgentWelcomeScreen>` 添加动态 class 以适配布局。5. TypeScript 编译通过。 - 2026年04月26日
VISION-FINAL-01-Backend-Service: 后端构建豆包多模态视觉解析服务与路由。1. 在 `gomoku-server/src/services/` 下新建 `vision.service.ts`，引入 `openai` 库。2. 使用环境变量 `VOLCENGINE_API_KEY`、`VOLCENGINE_BASE_URL` 和 `DOUBAO_VISION_ENDPOINT_ID` 初始化 OpenAI 客户端。3. 实现 `recognizeBoard(imageBase64: string)` 方法，包含严格的 System Prompt 限制输出格式。4. 在 `gomoku-server/src/routes/` 下新建 `vision.routes.ts`，新增 `/api/vision/recognize` POST 路由。5. 在 `routes/index.ts` 中注册 vision 路由。6. TypeScript 编译通过。 - 2026年04月26日
VISION-FINAL-02-Frontend-API: 前端创建视觉API调用接口层。1. 在 `gomoku-web/src/api/vision-api.ts` 中定义 `RecognitionResult` 接口（`boardType: string`, `pieces: number[][]`）和 `VisionApiResponse<T>` 泛型响应接口。2. 创建 `VisionApiService` 类，遵循项目现有 API 模式（class-based service + singleton export）。3. 实现 `recognizeBoardFromBase64(imageBase64: string): Promise<RecognitionResult>` 方法，使用 fetch POST 请求 `/vision/recognize` 端点，包含认证头和异常处理。4. TypeScript 编译通过。 - 2026年04月26日
VISION-FINAL-03-Chat-Input-UI: 前端输入框支持图片选中、粘贴与预览。1. 在 `AgentChatInput.vue` 中新增 `selectedImageBase64` 响应式状态和 `fileInputRef`。2. 实现 `fileToBase64` 函数将 File 对象转换为 Base64 字符串。3. 添加隐藏的 `<input type="file">` 元素，配合 Paperclip 图标按钮触发文件选择。4. 为外层容器绑定 `@paste` 事件拦截剪贴板图片。5. 当有选中图片时，在 textarea 上方渲染缩略图预览和删除按钮（X 图标）。6. 修改 `send` 事件参数为对象形式 `{ text: string; imageBase64: string | null }`。7. 更新 `AgentView.vue` 的 `handleSend` 方法适配新参数结构。8. 添加 i18n 词条：`attachImage`、`removeImage`、`selectedImagePreview`。9. TypeScript 编译通过。 - 2026年04月26日
VISION-FINAL-04-Board-Override: 前端引擎层增加强制覆盖残局的方法。1. 在 `AgentGomokuPanel.vue` 中新增 `loadBoardState(pieces: number[][])` 方法。2. 方法逻辑：深拷贝传入的 pieces 数组覆盖 board 状态，清空 moveHistory、winner、winningLine，重置 aiFirst 为 false。3. 遍历棋盘统计黑子(1)和白子(2)数量，根据规则推断当前轮次：黑子数 <= 白子数则轮到黑棋，否则轮到白棋。4. 通过 `defineExpose` 暴露 `loadBoardState` 方法供 `AgentView.vue` 调用。5. TypeScript 编译通过。 - 2026年04月26日
VISION-FINAL-05-AgentView-Workflow: 前端：AgentView 视觉解析工作流联动闭环。1. 在 `AgentView.vue` 中导入 `visionApi` 和 `BLACK/WHITE` 常量。2. 修改 `handleSend` 为 async 函数，处理图片输入分支。3. 若包含图片，先在聊天流显示用户消息（带 hasImage 标记），开启 isThinking 状态。4. 显示"正在使用豆包视觉模型解析棋局截图..."提示。5. 调用 `visionApi.recognizeBoardFromBase64()` 解析图片。6. 解析成功后调用 `enterGomokuMode()` 展开右侧棋盘，调用 `loadBoardState()` 覆盖棋盘状态。7. 统计棋子数量推断当前轮次，回复"棋谱已载入！根据盘面推演，当前轮到[黑/白]方落子，请继续。"。8. 在 `types/agent.ts` 中新增 `hasImage` 属性。9. 添加 i18n 词条：`visionImageSent`、`visionParsingBoard`、`visionBoardLoaded`、`visionParseFailed`。10. TypeScript 编译通过。 - 2026年04月26日
BUGFIX-VISION-01-Express-Limit: 后端：解除Express的JSON Payload体积限制。1. 在 `gomoku-server/src/app.ts` 中将 `express.json()` 修改为 `express.json({ limit: '50mb' })`。2. 将 `express.urlencoded({ extended: true })` 修改为 `express.urlencoded({ limit: '50mb', extended: true })`。3. 确保几兆的Base64图片字符串能够顺利进入路由层。4. TypeScript编译通过。 - 2026年04月26日
BUGFIX-VISION-02-Frontend-Image-Compress: 前端：添加图片压缩机制。1. 在 `AgentChatInput.vue` 中新增 `compressImage` 函数，使用 Canvas API 对图片进行等比缩放。2. 设置最大宽高限制为 1024px，JPEG 导出质量为 0.8。3. PNG 格式保持原格式导出。4. `fileToBase64` 方法改为调用 `compressImage`。5. 大幅减少 Base64 体积，从几MB降至几百KB，防止后端超载并提升 API 响应速度。6. TypeScript 编译通过。 - 2026年04月26日
BUGFIX-VISION-01-Global-Bridge: 创建跨页面的视觉解析数据桥梁。1. 在 `src/composables/useVisionBridge.ts` 中新建全局单例 composable。2. 导出 `pendingVisionBoard` 响应式 ref (number[][] | null, 默认 null)。3. 提供 `setVisionBoard(pieces)` 方法，内部使用 `pieces.map(row => [...row])` 深拷贝赋值。4. 提供 `consumeVisionBoard()` 方法，返回当前值并清空为 null。5. TypeScript 编译通过。 - 2026年04月26日
BUGFIX-VISION-02-AgentView-Route: 重构AgentView的解析成功跳转逻辑。1. 在 `AgentView.vue` 中引入 `useRouter` 和 `useVisionBridge`。2. 视觉解析成功后，不再调用 `enterGomokuMode()` 打开分屏，改为调用 `setVisionBoard(result.pieces)` 存入全局状态。3. 使用 `router.push({ name: 'game' })` 跳转到独立的五子棋对弈页面（路由 `/game`）。4. 移除了不再需要的 `BLACK`/`WHITE` 导入和棋子计数逻辑（该逻辑将在 GameView 挂载时处理）。5. TypeScript 编译通过，所有测试通过。 - 2026年04月26日
BUGFIX-VISION-03-Reactivity-Fix: 修复引擎层的数组响应式覆盖逻辑。1. 在 `GameView.vue` 中新增 `loadBoardState(pieces)` 方法。2. 使用 `board.value = pieces.map(row => [...row])` 进行深度响应式赋值，避免破坏响应式。3. 清空 `moveHistory.value = []`、`winner.value = EMPTY`、`winningLine.value = []`、`hintMove.value = null`。4. 根据盘面黑白子数量正确计算并赋值 `currentPlayer.value`（黑子数≤白子数时轮到黑方）。5. 自动进入分析模式 `isAnalysisMode.value = true`。6. TypeScript 编译通过。 - 2026年04月26日
BUGFIX-VISION-04-GameView-Mount: 完善五子棋页面挂载时的视觉数据消费与打谱模式切换。1. 验证 `onMounted` 钩子已正确调用 `useVisionBridge().consumeVisionBoard()` 并在返回棋子时调用 `loadBoardState()`。2. 修复 `loadBoardState` 函数，新增 `mode.value = 'pve'` 设置，确保分析模式按钮在控制面板中可见（PVE控件区域）。3. 用户现在可以正确切换出分析模式，AI不会自动落子。4. 通过TypeScript编译、184项单元测试、生产构建验证。 - 2026年04月26日
BUGFIX-VISION-07-Hardcore-Prompt: 后端引入基于锚点与思维链的强力视觉Prompt。1. 重构 `VISION_SYSTEM_PROMPT`，采用四步骤思维链引导：定位棋盘区域→建立坐标系→逐行扫描→确认输出结果。2. 明确要求AI忽略顶部/底部UI界面，只锁定木质棋盘区域。3. 要求结果包裹在 ```json 代码块中。4. 新增 `validateAndParseJson()` 方法，优先从markdown代码块提取JSON，支持带思考过程的回复解析。5. TypeScript编译通过。 - 2026年04月26日
BUGFIX-VISION-08-Constant-Mapping: 前端增加棋盘数据的常量映射适配。1. 在 `GameView.vue` 的 `loadBoardState(pieces)` 方法中新增双重循环映射转换。2. 后端传入值 1 映射为本地 BLACK 常量，2 映射为 WHITE 常量，0 映射为 EMPTY 常量。3. 使用 `mappedBoard` 数组存储转换结果后再赋值给 `board.value`，确保 Vue 响应式更新正确触发。4. TypeScript 编译通过。 - 2026年04月26日
BUGFIX-VISION-09-Upload-Guidance: 前端增加图片上传的规范引导提示。1. 在 `AgentChatInput.vue` 的图片预览区域下方新增引导提示文字。2. 新增 i18n key `imageUploadGuidance`，中英文双语支持。3. 提示文字使用语义化 Tailwind 类名，支持深色/浅色主题。4. TypeScript 编译通过。 - 2026年04月26日
BUGFIX-VISION-12-Zero-Temperature: 后端锁死大模型采样随机性以保证输出稳定性。1. 在 `vision.service.ts` 的 `client.chat.completions.create()` 调用中新增 `temperature: 0.0` 和 `top_p: 0.01` 参数。2. 这两个参数剥夺大模型生成结果时的发散性和随机性，迫使它每次都选择概率绝对最高的坐标。3. 彻底解决同一张图解析结果时好时坏的"抖动"问题。4. TypeScript 编译通过。 - 2026年04月27日
BUGFIX-VISION-13-Self-Correction-Prompt: 后端引入"先盘点后定位"的自校验闭环 Prompt。1. 重构 `vision.service.ts` 中的 `VISION_SYSTEM_PROMPT`。2. 新增四步严格思维链：【全局盘点】→【坐标系画图】→【自我校验】→【输出JSON】。3. 强制模型先统计黑子/白子总数，再画 ASCII 字符画，最后校验字符画中的棋子数量与盘点总数是否一致。4. 只有校验通过后才能输出 JSON，从根本上杜绝漏看或多看棋子的问题。5. TypeScript 编译通过。 - 2026年04月27日
BUGFIX-VISION-14-Smart-Auto-Crop: 前端增加针对竖屏截图的智能居中裁切算法。1. 重构 `AgentChatInput.vue` 中的 `fileToBase64` 函数，替换原有的 `compressImage` 为 `smartCropAndCompress`。2. 抽取 `loadImage` 辅助函数统一处理图片加载。3. 新增竖屏检测逻辑：当 height/width > 1.2 时判定为竖屏截图，自动从图片正中间裁切 width x width 的方形区域。4. 裁切后再按比例缩小到 max 1024，非竖屏图片保持原有按比例缩放逻辑。5. TypeScript 编译通过。 - 2026年04月27日
BUGFIX-VISION-15-Mathematical-Anchors: 后端在 Prompt 中注入绝对数学坐标锚点。1. 重构 `vision.service.ts` 中的 `VISION_SYSTEM_PROMPT` 的【坐标系画图】步骤。2. 明确列出 5 个星位的绝对坐标：左上角[3,3]、右上角[3,11]、天元[7,7]、左下角[11,3]、右下角[11,11]。3. 强制模型以星位为锚点，通过计算相对偏移量来定位棋子，禁止从边缘数格子。4. TypeScript 编译通过。 - 2026年04月27日
BUGFIX-VISION-16-Backend-Multi-Candidates: 后端修改 API 参数与 Prompt 生成 3 组候选方案。1. 将调用大模型 API 的 temperature 从 0.0 改为 0.4，top_p 从 0.01 改为 0.9，以允许产生合理的空间偏移变体。2. 修改 System Prompt：告知 AI 网格识别存在 1-2 格的物理偏移风险，要求输出 3 组最可能的候选方案。3. 更新 `BoardRecognitionResult` 接口，将 `pieces` 字段替换为 `candidates` (类型为 `number[][][]`)。4. 重构 `validateAndParseJson` 函数以验证多个候选矩阵。5. 增加 `max_tokens` 到 4000 以容纳更大的响应。6. TypeScript 编译通过。 - 2026年04月27日
BUGFIX-VISION-17-Frontend-Types-Bridge: 前端更新接口定义与跨页面数据传递逻辑。1. 修改 `vision-api.ts` 中 `RecognitionResult` 接口，将 `pieces: number[][]` 替换为 `candidates: number[][][]`。2. 重构 `useVisionBridge.ts`：将 `pendingVisionBoard` 改为 `pendingVisionCandidates`，方法名从 `setVisionBoard/consumeVisionBoard` 改为 `setVisionCandidates/consumeVisionCandidates`。3. 更新 `AgentView.vue` 调用新方法传递候选矩阵。4. 更新 `useGameState.ts`：`loadVisionBoard` 改为 `loadVisionCandidates`，默认加载第一个候选方案。5. 更新 `GameView.vue` 调用新方法。6. TypeScript 编译通过。 - 2026年04月27日
BUGFIX-VISION-18-Selector-And-Editor-UI: 前端实现方案选择与手动微调集成控制面板。1. 创建 `VisionConfirmationPanel.vue` 组件，在棋盘下方弹出识别结果确认悬浮窗。2. 面板上半部分提供『方案 1/2/3』切换按钮，点击调用 `selectCandidate` 刷新棋盘。3. 面板下半部分增加『进入手动微调』开关。4. 开启时显示微调工具栏：[落黑子、落白子、橡皮擦]。5. 在 `useGameState.ts` 中添加视觉确认状态管理（`visionCandidates`、`selectedCandidateIndex`、`isVisionEditMode`、`editTool`、`isVisionConfirming`）。6. 更新 `Board.vue` 支持编辑模式交互，根据 `editTool` 更新棋盘数组。7. 添加编辑模式悬浮预览：落子工具显示半透明棋子，橡皮擦显示红色高亮。8. 底部提供『确认无误，进入打谱』按钮。9. 添加相关 i18n 翻译键。10. TypeScript 编译通过，构建成功。 - 2026年04月27日
BUGFIX-VISION-19-Edit-Mode-Interactions: 优化微调模式下的棋盘交互体验。1. 修复编辑模式下普通悬浮预览与编辑预览冲突问题（添加 `!isVisionEditMode` 条件）。2. 优化落子工具悬浮预览：为白子添加边框使其更清晰可见。3. 优化橡皮擦工具悬浮预览：红色高亮覆盖棋子本身（w-[85%] h-[85%]）而非整个格子，添加半透明红色背景增强视觉效果。4. 验证编辑操作不触发 AI 思考或自动落子逻辑（`editBoardCell` 仅修改棋盘数据）。5. TypeScript 编译通过。 - 2026年04月27日
PORTAL-BG-01-Canvas-Grid: 新建棋盘粒子背景组件与基础网格系统。1. 在 `gomoku-web/src/components/` 目录下创建 `ChessParticleBackground.vue` 组件。2. 模板包含充满父容器的 `<canvas>` 元素。3. 实现窗口大小变化监听（resize），动态调整 canvas 的 width 和 height。4. 使用 ResizeObserver 监听父容器尺寸变化。5. 建立无形网格系统：cellSize = 50px，根据屏幕宽高计算 rows 和 cols。6. 使用 Map 存储网格占用状态，提供 `isCellOccupied`、`setCellOccupied`、`clearGrid` 方法。7. TypeScript 编译通过。 - 2026年04月28日
PORTAL-BG-02-Lifecycle-Render: 实现棋子的随机生成、消亡与 3D 拟真渲染。1. 定义 `ChessPiece` 接口：包含 row、col、color(black/white)、opacity、life、maxLife、isActive 属性。2. 实现 `requestAnimationFrame` 游戏循环，以 16ms 间隔更新和渲染。3. 实现随机生成逻辑：每帧以 0.002 概率在空闲网格生成新棋子。4. 实现生命周期管理：棋子生命值递减，透明度呈现"淡入(60帧)->保持->淡出(60帧)"效果。5. 实现 3D 拟真渲染：黑子使用径向渐变(深灰到纯黑)配合边框；白子使用径向渐变(亮灰到纯白)配合阴影效果。6. 激活状态棋子添加金色呼吸光晕特效。7. TypeScript 编译通过，Vite build 成功。 - 2026年04月28日
PORTAL-BG-03-Flood-Fill-Algorithm: 实现同色相邻棋子的激活算法 (Flood Fill)。1. 新增 `getPieceAt(row, col)` 辅助函数，根据坐标获取棋子实例。2. 实现 DFS-based `floodFillActivate(startRow, startCol)` 函数：使用栈结构遍历上下左右四个方向，查找所有相连的同色棋子。3. 激活逻辑：将连通块内所有棋子的 `isActive` 标记为 true，并重置生命周期（防止突然消失）。4. 增强渲染特效：为激活棋子添加双层金色呼吸光晕（外圈 + 中圈），并在棋子中心绘制呼吸效果的圆点。5. 通过 `defineExpose` 暴露 `floodFillActivate` 和 `getPieceAt` 方法供外部调用。6. TypeScript 编译通过，Vite build 成功。 - 2026年04月28日
PORTAL-BG-04-User-Interaction: 实现单击激活与双击生成事件的网格映射。1. 新增 `getGridPosition(offsetX, offsetY)` 函数，将鼠标坐标映射到网格坐标。2. 实现单击处理：使用 200ms 延迟区分单击与双击，单击时检查该位置是否有棋子，若有则触发 Flood Fill 激活算法。3. 实现双击处理：取消单击延迟计时器，检查目标位置是否为空，若为空则生成随机颜色新棋子并立即触发 Flood Fill 激活。4. 添加 `cursor-pointer` 样式提示可交互。5. 在 `onUnmounted` 中清理计时器防止内存泄漏。6. TypeScript 编译通过，Vite build 成功。 - 2026年04月28日
PORTAL-BG-05-View-Integration: 在登录注册页集成背景并优化层级。1. 在 `LoginView.vue` 中引入 `ChessParticleBackground` 组件，使用 `absolute inset-0 -z-10` 定位放置在最底层。2. 为表单卡片添加 `backdrop-blur-md` 磨砂玻璃效果，并将背景色调整为 `bg-stone-900/80` (dark) 和 `bg-white/80` (light) 以增强透明度和视觉层次。3. 为容器添加 `overflow-hidden` 防止背景溢出，为内容区域添加 `relative z-10` 确保层级正确。4. 由于登录页和注册页共用同一组件 (`LoginView.vue` 通过 `isLoginMode` 切换)，一次修改同时覆盖两个视图。5. TypeScript 编译通过，Vite build 成功。 - 2026年04月28日
AGENT-ROUTE-01-Bridge-Decoupling: 重构 useVisionBridge.ts 引入 Promise 异步拦截机制。1. 新增 `resolveConfirmation` 状态（类型为 `((pieces: number[][]) => void) | null`），用于持有 Promise 的 resolve 函数。2. 新增 `requestBoardConfirmation(candidates)` 方法：调用 `setVisionCandidates` 触发 UI 显示微调面板，同时返回一个 `Promise<number[][]>`，内部将 resolve 函数赋值给 `resolveConfirmation`，使调用方可以 await 等待用户确认结果。3. 新增 `confirmBoard(pieces)` 方法：执行并清空 `resolveConfirmation`，将最终棋盘数组返回给等待中的调用方。4. 新增辅助方法 `hasPendingConfirmation()` 检查是否有等待中的确认请求。5. 保留所有原有 API 不变，确保向后兼容。6. Vite build 成功。 - 2026年05月01日
AGENT-ROUTE-02-HITL-UI-Adaptation: 修改微调面板确认逻辑，接入异步确认回调。1. 在 `useGameState.ts` 的 `confirmVisionBoard()` 方法中，新增对 `useVisionBridge().hasPendingConfirmation()` 的检查。2. 若存在等待中的 HITL 确认请求（即 AgentView 通过 `requestBoardConfirmation` 发起的 Promise），调用 `confirmBoard(board.value)` 将用户微调后的棋盘数据交还给上层调用方。3. 若无等待中的确认请求，保留原有行为（清除视觉状态，棋盘留在分析模式）。4. 两种路径均会销毁微调面板（清除 visionCandidates、isVisionConfirming、选择状态等）。5. 面板组件 VisionConfirmationPanel 无需改动，确认事件仍由 GameView 的 handleConfirmVisionBoard 触发。6. 面板从"跳转器"变为纯粹的"数据清洗器"，不再隐式触发路由跳转。7. TypeScript lint 通过，Vite build 成功。 - 2026年05月01日
AGENT-ROUTE-03-Intent-Routing: 实现图文混合的意图路由与战术推演。1. 在 `useVisionBridge.ts` 中新增 `pendingQuestion`、`pendingImageBase64`、`pendingAnalysis` 模块级状态，以及 `setPendingQuestion`、`consumePendingQuestion`、`setPendingAnalysis`、`consumePendingAnalysis`、`clearPendingRequest` 方法，用于跨组件（跨路由）传递意图数据。2. 在 `useAgentChat.ts` 中导出 `executeStreamingChat` 方法，供 AgentView 的 onMounted 直接调用 LLM 流式接口。3. 在 `AgentView.vue` 的 `handleSend` 中实现意图分流：情况 A（仅图片）-> vision API -> `requestBoardConfirmation` -> navigate to /game；情况 B（图片+文本）-> vision API -> `setPendingQuestion` -> `requestBoardConfirmation` -> navigate to /game。4. 在 `useGameState.ts` 的 `confirmVisionBoard` 中，确认后检查 `consumePendingQuestion()`，若存在则将棋盘数据+问题存入 `pendingAnalysis`。5. 在 `GameView.vue` 中，确认后检查 `pendingAnalysis`，若存在则自动 navigate 回 agent 页面；取消时调用 `clearPendingRequest` 清理桥接状态。6. 在 `AgentView.vue` 的 `onMounted` 中检查 `consumePendingAnalysis()`，若存在则还原用户消息、构建组合 prompt（棋盘 JSON + 用户提问）、调用 `executeStreamingChat` 发送至 LLM，流式渲染战术分析文本。7. 新增 i18n key `visionAnalyzingPosition`。8. Vite build 成功。 - 2026年05月01日
AGENT-VISION-01-Vision-Confirm-State: 扩展对弈模式枚举与视觉确认状态管理。1. 将 PlayMode 联合类型从 `'chat' | 'gomoku'` 扩展为 `'chat' | 'gomoku' | 'vision-confirm'`。2. 新增响应式状态 `visionCandidates`（类型 `ref<number[][][] | null>`，默认 null）、`pendingImageBase64`（类型 `ref<string | null>`）、`pendingQuestion`（类型 `ref<string | null>`）。3. 新增 `enterVisionConfirmMode(candidates, imageBase64, question?)` 方法：设置三个新状态并将 playMode 设为 'vision-confirm'。4. 修改 `exitPlayMode()` 在重置 playMode 的同时清空三个新状态。5. useGlobalAgentPlay() 单例模式不受影响。6. TypeScript 类型检查无新增错误（已有预存 i18n 类型错误与本次改动无关）。 - 2026年05月01日
AGENT-VISION-02-Vision-Panel: 创建 AgentVisionPanel.vue 视觉确认分屏面板。1. 在 `gomoku-web/src/components/agent/` 下新建 `AgentVisionPanel.vue`，包含六个功能区：顶部信息栏（标题+关闭按钮）、候选方案切换按钮行、Board 棋盘渲染区（复用 Board.vue，传入 vision edit mode 相关 props）、微调工具栏（编辑模式开关+黑子/白子/橡皮擦工具）、批量移动折叠区（选区拖选+方向偏移+格数选择，逻辑参考 VisionConfirmationPanel.vue）、底部双按钮操作栏（进入打谱模式 primary 样式 + AI 战术分析 outline 样式）。2. Props: `candidates: number[][][]`、`imageBase64: string`。3. Emits: `confirm-replay`、`confirm-analysis`、`close`。4. DefineExpose: `getCurrentBoard()`、`loadCandidate(index)`。5. 新增 8 个 i18n key（agentVisionPanelTitle、agentVisionCandidateLabel、agentVisionEditBlack、agentVisionEditWhite、agentVisionEraser、agentVisionEditMode、agentVisionBatchMove、agentVisionConfirmReplay、agentVisionConfirmAnalysis）到 Messages 接口及中英文 locale 文件。6. 复用已有的 visionBatchMoveHint/visionMoveOffset/visionClearSelection/visionSelectionActive key。7. TypeScript lint + Vite build 均通过。 - 2026年05月01日
AGENT-VISION-03-Vision-Integration: AgentView 集成视觉确认分屏与图片发送流程重构。**Part 1 - 分屏布局集成**：1. 将 `isGomokuLayout` 计算属性重命名为 `isSplitLayout`，新增 `playMode === 'vision-confirm'` 条件触发分屏布局。2. 右侧面板根据 playMode 条件渲染：gomoku 模式渲染 AgentGomokuPanel，vision-confirm 模式渲染 AgentVisionPanel（传入 visionCandidates 和 pendingImageBase64 props）。3. 拖拽分割线在视觉确认模式下同样生效（由 isSplitLayout 控制）。4. 为 AgentVisionPanel 绑定 @confirm-replay、@confirm-analysis、@close 三个事件（stub handlers，具体逻辑由后续 story 实现）。5. 左侧面板返回按钮、欢迎页、消息列表、输入区域的布局类统一使用 isSplitLayout 条件判断。**Part 2 - 图片发送流程重构**：1. handleSend 中 payload.imageBase64 分支重构：移除原有的 router.push({ name: 'game' }) 跳转和 bridge 调用（requestBoardConfirmation、setPendingQuestion）。2. 替换为调用 enterVisionConfirmMode(result.candidates, payload.imageBase64, userText || undefined) 直接在当前页进入视觉确认分屏。3. 在聊天流中追加 agentVisionConfirmEntered 系统消息。4. 保留 visionParsingBoard 的 isThinking 提示。5. 移除 useRouter 导入（不再需要）。6. 新增 i18n key agentVisionConfirmEntered 到 Messages 接口及中英文 locale 文件。7. Vite build 通过。 - 2026年05月01日
AGENT-VISION-04-Replay-Routing: 实现「进入打谱模式」分支流转闭环。**useVisionBridge.ts**：新增 `setVisionCandidatesForReplay(candidates: number[][][])` 方法，将用户微调后的唯一棋盘深拷贝写入 `pendingVisionCandidates` 状态，供 GameView 的 `consumeVisionCandidates()` 消费。**AgentView.vue**：1. 重新引入 `useRouter`，从 `useVisionBridge` 解构出 `setVisionCandidatesForReplay`。2. 实现 `handleConfirmReplay(pieces: number[][])` 方法：调用 `setVisionCandidatesForReplay([pieces])` 写入桥接数据 → 调用 `exitPlayMode()` 退出分屏 → 执行 `router.push({ name: 'game' })` 跳转到五子棋独立对弈页面。3. GameView 的现有 `loadVisionCandidates()` 逻辑自动消费桥接数据并进入打谱模式，无需修改 GameView。Vite build 通过。 - 2026年05月01日
AGENT-VISION-05-AI-Analysis-Branch: 实现「AI 战术分析」分支：确认后留在智能体页面进行 LLM 对话分析。**AgentView.vue**：1. 实现 `handleConfirmAnalysis(pieces: number[][])` 方法，绑定到 AgentVisionPanel 的 `@confirm-analysis` 事件。2. 方法逻辑：a. 从 useGlobalAgentPlay 读取 pendingImageBase64 和 pendingQuestion（在 exitPlayMode 清空前），设置 isExitingGomoku 触发平滑过渡动画，调用 exitPlayMode() 退出分屏。b. 遍历 pieces 统计黑子（cell===1）和白子（cell===2）数量，根据黑子数<=白子数判断当前轮到黑方，否则轮到白方，生成棋盘概况消息。c. 在聊天流中追加 Agent 消息，包含用户截图（imageBase64）和概况文本。d. 构造增强 Prompt：pieces 序列化为 JSON，有提问时拼接「请结合数据回答：${question}」，无提问时使用默认分析 Prompt。e. 调用 executeStreamingChat 发送给 LLM，流式响应在左侧聊天界面正常渲染。3. isExitingGomoku 延迟机制复用于 vision-confirm 模式，退出后 400ms 重置。4. i18n 新增 key：agentVisionAnalysisSummary（棋盘概况模板）、agentVisionDefaultAnalysis（默认分析提问），已添加到 Messages 接口及中英文 locale 文件。Vite build 通过。 - 2026年05月01日
AGENT-VISION-06-Vision-Confirm-Close: 实现视觉确认面板关闭与取消流程。**AgentView.vue**：1. 实现 `handleVisionConfirmClose()` 方法，绑定到 AgentVisionPanel 的 `@close` 事件（已有桩函数）。2. 方法逻辑：a. 设置 isExitingGomoku 为 true 触发平滑过渡动画。b. 调用 exitPlayMode() 退出分屏，playMode 回归 chat，清空 visionCandidates/pendingImageBase64/pendingQuestion。c. 调用 clearPendingRequest() 清理 useVisionBridge 中的跨路由残留状态。d. 在聊天流中追加 Agent 系统消息，使用 i18n key agentVisionConfirmCancelled。e. nextTick 滚动到底部。f. 400ms 延迟重置 isExitingGomoku。3. i18n 新增 key：agentVisionConfirmCancelled（zh-CN: "棋谱确认已取消", en-US: "Board confirmation has been cancelled."），已添加到 Messages 接口及中英文 locale 文件。Vite build 通过。 - 2026年05月01日
AGENT-VISION-07-VisionBridge-Cleanup: 清理 useVisionBridge 遗留逻辑并确保双向兼容。**useVisionBridge.ts**：1. `requestBoardConfirmation` 添加 Legacy 注释，标记为仅供 GameView 直接跳转旧流程使用，新 AgentVisionPanel 流程不再调用。2. `setVisionCandidatesForReplay` 已在 AGENT-VISION-04 实现，验证其正确深拷贝 candidates 到 pendingVisionCandidates，与 GameView 的 consumeVisionCandidates 完全兼容。3. `clearPendingRequest()` 新增清理 `pendingVisionCandidates` 状态，确保取消/关闭流程不残留跨路由数据。4. `consumeVisionCandidates()` 无需修改，GameView 的 loadVisionCandidates 正常消费。5. `pendingAnalysis`、`PendingAnalysis` 接口、`setPendingAnalysis`、`consumePendingAnalysis` 全部标记 @deprecated，保留不删除以维护旧 GameView→AgentView 跳转流程的向后兼容。TypeScript 类型检查通过（无新增错误）。 - 2026年05月01日
AGENT-VISION-08-i18n-Keys-And-Smoke-Test: 添加 i18n 翻译键与全链路冒烟测试。**i18n 翻译键验证**：1. 确认全部 13 个 agentVision* i18n key 已在 zh-CN.json/en-US.json 中定义且翻译正确（agentVisionPanelTitle, agentVisionConfirmEntered, agentVisionCandidateLabel, agentVisionEditBlack, agentVisionEditWhite, agentVisionEraser, agentVisionEditMode, agentVisionBatchMove, agentVisionConfirmReplay, agentVisionConfirmAnalysis, agentVisionAnalysisSummary, agentVisionDefaultAnalysis, agentVisionConfirmCancelled）。2. 确认所有 key 已在 Messages 接口中声明（lines 264-276）。3. 确认所有 key 在 AgentVisionPanel.vue（10个key）和 AgentView.vue（3个key）中正确引用，无孤立key。**Messages 接口补全**：补充 9 个遗漏的类型声明（agentAiFirst, agentAiSecond, agentAiFirstMoveNotification, agentChatReasoningSummary, visionBatchMove, visionBatchMoveHint, visionMoveOffset, visionClearSelection, visionSelectionActive, exportBoard），使 Messages 接口与 JSON locale 文件完全对齐。**质量检查**：TypeScript lint (tsc --noEmit) 通过，Vite build 通过（3201 modules），全部单元测试通过（vitest）。 - 2026年05月01日
CHESS-VISION-01-Backend-Chess-Vision-Service: 创建中国象棋视觉识别服务。**gomoku-server/src/services/chess-vision.service.ts**（约200行）：1. ChessVisionService 类，复用 Doubao (Volcengine) 多模态 API，共用 VOLCENGINE_API_KEY / VOLCENGINE_BASE_URL / DOUBAO_VISION_ENDPOINT_ID 环境变量，使用 OpenAI SDK 客户端。2. 数据格式：识别结果为 10×9 二维数组，棋子编码 0-14（0=空位，1-7=红方帅仕相马车炮兵，8-14=黑方将士象马车炮卒）。3. System Prompt：声明为中国象棋视觉标定系统，通过楚河汉界文字和九宫格斜线确认棋盘类型，坐标约定 row0=黑方底线/row9=红方底线，通过中文字符识别棋子类型和阵营，自校验红帅1/黑将1/其余棋子上限约束，输出3组候选矩阵（原图+上偏移1格+下偏移1格）。4. parseVisionResponse：两阶段JSON提取（markdown code block → raw JSON）。5. validateAndParseJson：校验每个候选矩阵恰好10行9列、值域0-14整数。6. 导出单例 chessVisionService。TypeScript 编译通过。 - 2026年05月02日
CHESS-VISION-02-Backend-Unified-Vision-Service: 创建统一视觉识别入口，自动检测棋局类型。**gomoku-server/src/services/unified-vision.service.ts**（新增约100行）：1. UnifiedVisionService 类，复用 Doubao 视觉 API，共用环境变量。2. detectBoardType(imageBase64)：向视觉模型发送极短检测 Prompt（含 gomoku/chinese_chess 特征描述），max_tokens=10, temperature=0 极速返回，解析响应字符串判断类型。3. recognizeBoard(imageBase64)：先调用 detectBoardType 获取类型 → 若为 chinese_chess 委托给 chessVisionService.recognizeChessBoard → 否则委托给 visionService.recognizeBoard → 统一返回 { boardType, candidates }。4. 导出单例 unifiedVisionService。**gomoku-server/src/routes/vision.routes.ts**（改动2行）：import 替换为 unifiedVisionService，路由处理函数调用 unifiedVisionService.recognizeBoard 替代原来直接调用 visionService.recognizeBoard。前端无需任何改动即可同时支持两种棋局。TypeScript 编译通过。 - 2026年05月02日
CHESS-VISION-03-Frontend-BoardType-Adaptation: 适配统一视觉 API 返回的 boardType 字段。**gomoku-web/src/api/vision-api.ts**（改动2行）：1. 新增 `BoardType` 联合类型 `'gomoku' | 'chinese_chess'`。2. 将 `RecognitionResult.boardType` 从 `string` 收窄为 `BoardType`。**gomoku-web/src/api/chess-vision-api.ts**（新增约80行）：1. `CHESS_PIECE_CODES` 常量对象（as const），定义 0-14 的棋子编码映射（0=EMPTY, 1-7=红方帅仕相马车炮兵, 8-14=黑方将士象马车炮卒）。2. `ChessPieceCode` 类型推导。3. `CODE_TO_PIECE` 映射表，使用 PieceType/PlayerSide 枚举值。4. `convertCodesToBoardState(codes: number[][]): BoardState` 函数：将 10x9 数值矩阵转换为 BoardState，每个非空棋子自动设置 coord。5. `convertBoardStateToCodes(board: BoardState): number[][]` 函数：反向转换，使用 Map 查找编码。TypeScript 编译通过。 - 2026年05月02日
CHESS-VISION-04-Frontend-useAgentPlay-Chess-Vision-Confirm: 扩展 useAgentPlay 支持象棋视觉确认模式。**gomoku-web/src/composables/useAgentPlay.ts**（改动约30行）：1. PlayMode 联合类型从 `'chat' | 'gomoku' | 'vision-confirm'` 扩展为 `'chat' | 'gomoku' | 'vision-confirm' | 'chess-vision-confirm'`。2. 新增响应式状态 `chessVisionCandidates: ref<number[][][] | null>`（默认 null），用于存储 3 组 10x9 候选棋盘矩阵。3. 新增方法 `enterChessVisionConfirmMode(candidates, imageBase64, question?)`：设置 playMode 为 chess-vision-confirm，写入 chessVisionCandidates 并复用 pendingImageBase64/pendingQuestion。4. 修改 `exitPlayMode()` 同时清空 chessVisionCandidates。5. 所有新增状态和方法均已导出，useGlobalAgentPlay() 单例模式不受影响。TypeScript 编译通过。 - 2026年05月02日
CHESS-VISION-05-Frontend-useVisionBridge-Chess-Data-Passing: 扩展 useVisionBridge 支持中国象棋数据传递。**gomoku-web/src/composables/useVisionBridge.ts**（改动约50行）：1. 导入 BoardState 类型。2. 新增 `pendingChessVisionCandidates: ref<number[][][] | null>`（默认 null），用于在 AgentView 和 ChineseChessView 之间传递 10x9 象棋棋盘矩阵。3. 新增 `ChessAnalysisData` 接口和 `pendingChessAnalysis: ref<ChessAnalysisData | null>` 响应式变量，存储象棋专用分析数据（board, currentPlayer, question, imageBase64）。4. 新增 `setChessVisionCandidatesForReplay(candidates)` 方法：深度拷贝 candidates 赋值给 pendingChessVisionCandidates。5. 新增 `consumeChessVisionCandidates()` 方法：读取并清空 pendingChessVisionCandidates。6. 新增 `setChessAnalysis(data)` 和 `consumeChessAnalysis()` 方法。7. 修改 `clearPendingRequest()` 同时清空 pendingChessVisionCandidates 和 pendingChessAnalysis。所有新增状态和方法均已导出。TypeScript 编译通过。 - 2026年05月02日
CHESS-VISION-06-Frontend-AgentView-Image-Upload-Routing: AgentView 图片上传流程改造，自动路由棋局类型。**gomoku-web/src/views/AgentView.vue**（改动约6行）：1. 从 useGlobalAgentPlay 解构新增 `enterChessVisionConfirmMode`。2. 修改 handleSend 中图片处理逻辑（约第371行），将原来无条件调用 `enterVisionConfirmMode` 改为根据 `result.boardType` 路由：若 `boardType === 'chinese_chess'` 调用 `enterChessVisionConfirmMode`，否则调用原有的 `enterVisionConfirmMode`。后端统一检测确保前端只需一次 API 调用，通过返回的 boardType 字段决定分支。TypeScript 编译通过。 - 2026年05月02日
CHESS-VISION-07-Frontend-AgentChessVisionPanel: 创建 AgentChessVisionPanel 象棋棋谱微调面板。**gomoku-web/src/components/agent/AgentChessVisionPanel.vue**（新建，约545行）：1. 引入中国象棋 Board 组件渲染 10x9 棋盘，维护内部 number[][] 棋盘矩阵，通过 convertCodesToBoardState 将数值编码（0-14）转换为 BoardState（Piece | null）[][] 格式。2. 候选方案切换：棋盘上方渲染方案 1/2/3 按钮，点击加载对应候选矩阵。3. 微调工具栏：编辑模式开关 + 红方工具（帅仕相马车炮兵 codes 1-7）+ 黑方工具（将士象马车炮卒 codes 8-14）+ 橡皮擦，通过监听 Board 的 clickCell/selectPiece/movePiece 事件实现编辑。4. 批量移动：点击式选区（两次点击确定矩形区域）+ 方向按钮 + 偏移 1-2 格，适配 10x9 边界检查。5. 底部操作栏：「进入打谱模式」（primary）+「AI 战术分析」（outline + html2canvas 截图）。6. 顶部信息栏：标题 + 关闭按钮。Props: candidates/imageBase64，Emits: confirm-replay/confirm-analysis/close，DefineExpose: getCurrentBoard/loadCandidate。**gomoku-web/src/i18n.ts**：新增 chessVisionPanelTitle/chessVisionEditRedPieces/chessVisionEditBlackPieces 类型。**gomoku-web/src/locales/zh-CN.json + en-US.json**：新增 3 个翻译键。TypeScript 编译通过，Vite 构建通过。 - 2026年05月02日
CHESS-VISION-08-Frontend-GameState-loadBoardState: 为中国象棋 GameState 增加 loadBoardState 方法。**gomoku-web/src/games/chinese-chess/composables/useGameState.ts**（改动约25行）：1. 新增 `loadBoardState(codes: number[][])` 方法，接收 10x9 数值矩阵，调用 `convertCodesToBoardState` 转换为 BoardState 并直接赋值 board.value。2. 重置 moveHistory/selectedPiece/validMoves/moveResult 为空。3. 遍历棋盘统计红黑棋子数量推断当前回合（红方棋子总数 <= 黑方棋子总数则轮到红方）。4. 设置 gameStatus 为 IN_PROGRESS，mode 为 pvp。**gomoku-web/src/games/chinese-chess/utils.ts**（新建）：从 chess-vision-api.ts 重新导出 convertCodesToBoardState 和 convertBoardStateToCodes，避免重复实现。TypeScript 编译通过（无新增错误）。 - 2026年05月02日
CHESS-VISION-09-Frontend-AgentView-ChessVision-Integration: AgentView 集成象棋视觉确认分屏与完整流程。**gomoku-web/src/views/AgentView.vue**（改动约70行）：1. 引入 AgentChessVisionPanel 组件。2. 从 useGlobalAgentPlay 解构新增 chessVisionCandidates。3. 从 useVisionBridge 解构新增 setChessVisionCandidatesForReplay。4. isSplitLayout 计算属性增加 `chess-vision-confirm` 条件。5. 返回按钮 v-if 条件增加 `chess-vision-confirm`。6. 右侧面板容器 v-if 条件增加 `chess-vision-confirm`。7. 新增 AgentChessVisionPanel 渲染分支，传入 candidates/imageBase64 props，绑定 confirm-replay/confirm-analysis/close 事件。8. 实现 handleChessConfirmReplay：调用 setChessVisionCandidatesForReplay + exitPlayMode + router.push 跳转 chinese-chess 页面。9. 实现 handleChessConfirmAnalysis：退出分屏，推送用户消息（含截图），构造增强 Prompt（含棋子编码说明 0-14），调用 executeStreamingChat 进行 AI 战术分析。10. 实现 handleChessVisionConfirmClose：退出分屏，推送取消消息，清理状态。TypeScript 编译通过（无新增错误）。 - 2026年05月02日
CHESS-VISION-10-Frontend-ChineseChessView-Vision-Consumption: ChineseChessView 消费视觉数据并加载残局。**gomoku-web/src/games/chinese-chess/views/ChineseChessView.vue**（改动约20行）：1. 新增 onActivated 生命周期导入。2. 引入 useVisionBridge 和 PlayerSide 类型。3. 新增 visionConsumed 标志位避免 keep-alive 场景下重复消费。4. 实现 consumeAndLoadVision 函数：调用 consumeChessVisionCandidates 获取候选方案，取 candidates[0] 通过 gameState.loadBoardState 加载残局，根据 currentPlayer 判断红/黑方文本并调用 gameUI.notify 提示。5. 在 onMounted 和 onActivated 中均调用 consumeAndLoadVision。复用已有 i18n key（visionBoardLoaded/chessRed/chessBlack）。TypeScript 编译通过（无新增错误）。 - 2026年05月02日
CHESS-VISION-11-Frontend-ChineseChessView-AI-Tactical-Analysis: ChineseChessView 增加一键 AI 战术分析功能。**gomoku-web/src/games/chinese-chess/components/GameControls.vue**（改动约20行）：1. 新增 Sparkles 图标导入。2. emit 新增 requestAiAnalysis 事件。3. 在工具栏中新增「AI 战术分析」按钮，游戏进行中且非 AI 思考时可用，disabled 条件为 winner !== undefined || isAiThinking。**gomoku-web/src/games/chinese-chess/views/ChineseChessView.vue**（改动约25行）：1. 引入 useRouter 和 convertBoardStateToCodes。2. 实现 handleRequestAiAnalysis：使用 html2canvas 截图 boardRef 生成 boardImageBase64，调用 convertBoardStateToCodes 转换棋盘为数值矩阵，通过 useVisionBridge().setChessAnalysis 存储分析数据（board/currentPlayer/question/imageBase64），router.push 跳转 agent 页面。**gomoku-web/src/views/AgentView.vue**（改动约30行）：1. 从 useVisionBridge 解构新增 consumeChessAnalysis。2. 引入 convertBoardStateToCodes 和 PlayerSide。3. processPendingAnalysis 方法新增象棋分析优先消费：调用 consumeChessAnalysis 获取数据，构造含棋子编码说明的增强 Prompt，调用 executeStreamingChat 流式分析。**gomoku-web/src/i18n.ts + locales**（改动约6行）：新增 requestChessAiAnalysis 和 chessVisionDefaultAnalysis 翻译 key。TypeScript 编译通过（无新增错误）。 - 2026年05月02日
CHESS-VISION-12-Frontend-i18n-Chess-Vision-Translation-Keys: 添加中国象棋视觉识别相关 i18n 翻译键。**gomoku-web/src/locales/zh-CN.json**（新增9个key）：chessVisionEditEraser、chessVisionEditMode、chessVisionBatchMove、chessVisionConfirmReplay、chessVisionConfirmAnalysis、chessVisionAnalysisSummary、chessVisionConfirmCancelled、chessVisionBoardConfirmed、chessVisionParseFailed。**gomoku-web/src/locales/en-US.json**（对应9个英文翻译）。**gomoku-web/src/i18n.ts**（Messages 接口新增9个类型声明）。其中 chessVisionPanelTitle、chessVisionEditRedPieces、chessVisionEditBlackPieces、requestChessAiAnalysis、chessVisionDefaultAnalysis 已在 CHESS-VISION-11 中添加，本次补齐剩余 key。TypeScript 编译通过（无新增错误）。 - 2026年05月02日
CHESS-VISION-13-Full-Chain-Smoke-Test: 全链路冒烟测试与功能验证。通过代码级审查验证5条路径：(1) 五子棋回归：handleSend -> recognizeBoardFromBase64 -> boardType !== chinese_chess -> enterVisionConfirmMode -> AgentVisionPanel -> confirm-replay/confirm-analysis/close 三条事件路径完整。(2) 象棋自动识别：boardType === chinese_chess -> enterChessVisionConfirmMode -> AgentChessVisionPanel -> 候选方案切换/编辑工具/确认打谱/分析 -> ChineseChessView consumeAndLoadVision 加载残局。(3) AI战术分析：handleChessConfirmAnalysis -> exitPlayMode -> 构造含编码说明的增强Prompt -> executeStreamingChat。(4) 取消流程：handleChessVisionConfirmClose -> exitPlayMode + clearPendingRequest -> 退回chat。(5) 一键AI分析：ChineseChessView handleRequestAiAnalysis -> setChessAnalysis -> router.push agent -> processPendingAnalysis consumeChessAnalysis -> executeStreamingChat。修复两个小问题：(a) ChineseChessView.vue 移除未使用的 boardCodes 变量及 import；(b) AgentView.vue processPendingAnalysis 中 executeStreamingChat 调用补加 await。TypeScript 编译通过，184 测试全部通过。 - 2026年05月02日
CHESS-LLM-001-Types: 创建中国象棋 LLM AI 服务类型定义。新增 **gomoku-server/src/types/chess-llm.types.ts**，定义 5 个类型：ChessPosition（棋盘坐标 row/col）、ChessPlayerColor（red/black 联合类型）、ChessMoveRecord（历史走法记录含 from/to/piece/capturedPiece/notation）、ChessLLMMoveRequest（LLM 走子请求含 board 10x9 矩阵/currentPlayer/moveHistory）、ChessLLMMoveResponse（LLM 走子响应含 move/reason/situationAnalysis/isFallback）、ChessCandidateMove（候选走法含 from/to/score/reason）。在 **gomoku-server/src/types/index.ts** 中添加 re-export。棋子编码 0-14 与视觉识别模块保持一致。TypeScript 编译通过（无新增错误）。 - 2026年05月02日
CHESS-LLM-002-Board-Prompt: 编写中国象棋棋盘文本化与 Prompt 组装工具。新增 **gomoku-server/src/utils/chessBoardPromptUtils.ts**（约160行）。实现3个函数：(1) boardToAscii(board) - 将10x9数值矩阵转为人类可读ASCII棋盘，红方用标准中文字符（帅仕相马车炮兵），黑方用变体字符（将士象馬車砲卒），空位用·，标注楚河汉界，列号a-i行号0-9。(2) getMaterialAnalysis(board) - 统计双方子力分值（车900、马400、炮450、仕200、相200、兵100、过河兵+100），输出红黑双方总分对比及优劣势文本。(3) buildChessSystemPrompt(board, currentPlayer, candidates) - 组装完整LLM系统Prompt，包含角色设定、棋盘ASCII可视化、子力分析、候选走法Top15列表、策略指导（优先将军/应将、关注子力交换、将帅安全）、强制JSON输出格式定义。TypeScript 编译通过。 - 2026年05月02日
CHESS-LLM-003-Candidate-Generator: 实现中国象棋候选走法生成器。新增 **gomoku-server/src/utils/chessCandidateGenerator.ts**（约310行）。核心函数 `generateCandidateMoves(board, playerColor)` 返回 Top 15 候选走法。实现7种棋子伪合法走法生成：车（直线扫描遇子停）、马（日字跳+蹩马腿检测）、炮（直线移动/隔一子吃）、兵卒（过河前只能前进，过河后可左右）、仕士（九宫格内斜线一步）、相象（田字走法+塞象眼+不过河）、帅将（九宫格内直线一步）。合法走法过滤：模拟走后检查己方将帅不被将军（遍历对方所有棋子攻击检测）+ 将帅不对面。走法评分启发式：吃子价值（车900/炮450/马400/仕200/相200/兵100）+ 将军加分500 + 中心控制加分50 + 过河兵前进加分30。记谱法生成：红方用中文数字（一二三...），黑方用阿拉伯数字，支持进/退/平方向标注及吃子/将军括号注释。额外导出 `isInCheck`、`areKingsFacing`、`findKing`、`applyMove`、`generateAllLegalMoves`、`isCheckmate` 等工具函数供后续 CHESS-LLM-004 威胁检测器复用。TypeScript 编译通过（零新增错误）。 - 2026年05月02日
CHESS-LLM-004-Threat-Detector: 实现中国象棋关键威胁检测拦截器。新增 **gomoku-server/src/utils/chessThreatDetector.ts**（约200行）。核心函数 `findCriticalMove(board, playerColor)` 按3级优先级检测关键走法：(1) 绝杀检测 — 遍历己方所有合法走法，模拟走后调用 `isCheckmate` 检测对方是否被将死；(2) 应将检测 — 当己方被将军时，遍历合法走法按优先级分类（吃掉将军子 > 垫将/挡将 > 帅/将回避），通过移除目标位置棋子后检查是否仍被将军来识别将军源；(3) 对方致命威胁拦截 — 模拟对方所有合法走法检测是否存在一步绝杀，若存在则遍历己方走法寻找能阻止所有绝杀路径的走法（吃威胁子 > 拦截 > 帅/将回避），通过 `preventsAnyCheckmate` 辅助函数验证走法有效性。导出 `CriticalMoveResult` 接口和 `findCriticalMove` 函数供 CHESS-LLM-005 服务层调用。复用 chessCandidateGenerator 的 `generateAllLegalMoves`、`applyMove`、`isInCheck`、`isCheckmate`、`PIECE_NAMES`、`PIECE_VALUES`、`isEnemy`。TypeScript 编译通过（零新增错误）。 - 2026年05月02日
CHESS-LLM-005-Service: 实现中国象棋 LLM 服务（DeepSeek API 调用与幻觉校验）。新增 **gomoku-server/src/services/chess-llm.service.ts**（约190行）。核心类 `ChessLLMService`（导出单例 `chessLlmService`）。`generateMove(request)` 方法流程：(1) 威胁拦截 — 调用 `findCriticalMove` 检测绝杀/应将/致命威胁，若命中直接返回跳过 LLM（`isFallback: true`）；(2) 候选走法生成 — 调用 `generateCandidateMoves` 获取 Top 15 候选；(3) Prompt 组装 — 调用 `buildChessSystemPrompt` 构建含棋盘 ASCII、子力分析、候选列表的完整 Prompt；(4) DeepSeek API 调用 — 复用环境变量 `DEEPSEEK_API_KEY/BASE_URL/MODEL`，temperature=0.3，raw fetch 非流式调用；(5) 响应解析与幻觉校验 — 解析 LLM JSON 提取 move(from/to)，先检查是否在候选列表中，若不在则通过 `generateAllLegalMoves` 验证走法合法性（from 有己方棋子、走法在合法列表中），非法时使用评分最高候选兜底并标注「大模型返回非法走法，已启用兜底策略」，合法但非候选则直接接受。`healthCheck()` 通过 GET /models 验证 API 连通性。复用 chessCandidateGenerator、chessThreatDetector、chessBoardPromptUtils 工具模块。TypeScript 编译通过（零新增错误）。 - 2026年05月02日
CHESS-LLM-006-Controller-Routes: 创建中国象棋 LLM 控制器与路由。新增 **gomoku-server/src/controllers/chess-llm.controller.ts**（约90行）。`ChessLLMController` 类实现两个端点：(1) `generateMove` — POST `/api/games/chinese-chess/llm/move`，校验请求体（board 必须为 10×9 矩阵、currentPlayer 为 red/black、moveHistory 可选数组），调用 `chessLlmService.generateMove()` 返回统一 ApiResponse；(2) `healthCheck` — GET `/api/games/chinese-chess/llm/health`，调用 `chessLlmService.healthCheck()` 验证 DeepSeek API 连通性，返回 healthy/degraded 状态。修改 **gomoku-server/src/routes/games/chinese-chess.routes.ts**，新增 `chessLlmController` 引用及两个路由注册（`GET /llm/health`、`POST /llm/move`），路由链为 `/api/games/chinese-chess/llm/*`，复用现有 `games/index.ts` 挂载无需额外修改。遵循 `llm-ai.controller.ts` 的 Controller 类模式（try/catch + ApiResponse 统一响应）。TypeScript 编译通过（零新增错误）。 - 2026年05月02日
CHESS-LLM-007-Frontend-API: 前端数据层 — 创建中国象棋 LLM API 客户端与 Composable 改造。新增 **gomoku-web/src/games/chinese-chess/api/chessLlmApi.ts**（约95行）。`ChessLLMApiService` 类遵循五子棋 `LLMApiService` 模式（class + singleton export），包含：(1) `generateMove(request)` — POST `/api/games/chinese-chess/llm/move`，请求体含 `board`(10×9 数值矩阵)、`currentPlayer`('red'/'black')、`moveHistory`，响应包裹在统一 `ApiResponse<T>` 中校验 success 后返回 data；(2) `healthCheck()` — GET `/api/games/chinese-chess/llm/health`，返回 boolean（成功/异常均不抛出）。前端类型 `ChessLLMMoveRequest/Response/Position/MoveRecord` 完整镜像后端 `chess-llm.types.ts`。修改 **gomoku-web/src/games/chinese-chess/composables/useGameAI.ts**，新增：(1) `isLlmThinking: Ref<boolean>` 响应式状态；(2) `llmMove(board, aiPlayerSide, onMove)` 方法 — 调用 `convertBoardStateToCodes()` 将 `BoardState` 转为数值矩阵 → 构造请求体（currentPlayer 由 PlayerSide 枚举映射）→ 调用 `chessLlmApi.generateMove()` → 解析响应 `move.from/to` 转为前端 `BoardCoord` → 通过 `onMove(ChessLLMMoveResult)` 回调（含 reason、situationAnalysis、isFallback）→ catch 时重置 isLlmThinking；(3) 导出 `ChessLLMMoveResult` 接口。原有 Web Worker AI（`aiMove`/`showHint`/`terminateWorker`）逻辑完全保留，`llmMove` 与之并行共存。TypeScript 编译通过（零新增错误）。 - 2026年05月02日
CHESS-LLM-008-UI-Integration: 前端视图层集成 — GameControls 与 ChineseChessView 集成神经网络 AI 选项与思考 UI。修改 **gomoku-web/src/games/chinese-chess/types.ts**，将 `Difficulty` 类型从 `'beginner'|'intermediate'|'advanced'|'expert'` 扩展为包含 `'neural'`。修改 **gomoku-web/src/api/game-api.ts**，将 `ChineseChessFrontendGame.aiDifficulty` 类型同步扩展包含 `'neural'`。修改 **gomoku-web/src/games/chinese-chess/composables/useGameUI.ts**：(1) `getAiDifficultyText` 新增 `'neural'` 分支返回 `t('difficultyNeural')`；(2) `getStatusText` 新增可选参数 `isLlmThinking`，当 LLM 思考时状态栏显示「DeepSeek 正在推演局势...」而非默认的「电脑正在思考...」。修改 **gomoku-web/src/games/chinese-chess/views/ChineseChessView.vue**（核心变更约80行）：(1) 引入 `ThinkingProcess.vue` 共享组件；(2) 新增 `llmThinkingContent`、`llmSituationContent`、`showLlmThinking`、`showLlmSituation` 响应式状态及 `isAnyAiThinking` 计算属性；(3) `triggerAiMove` 方法增加 neural 分支 — 当难度为 `'neural'` 时调用 `gameAI.llmMove()` 替代本地 Worker AI，LLM 返回后自动执行走法并保存 `reason`/`situationAnalysis` 用于展示；(4) `handleSelectPiece`/`handleMovePiece`/`handleClickCell` 增加 `isLlmThinking` 守卫，LLM 思考期间禁止棋盘操作；(5) `handleResetGame` 清空 LLM 分析内容；(6) 右侧面板在历史记录上方渲染两个 `ThinkingProcess` 组件（分别展示 AI 思路和局势评估），支持折叠/展开和字符统计；(7) GameControls 的 `isAiThinking` prop 改用 `isAnyAiThinking`，确保 neural 模式下同样禁用提示按钮等控件。TypeScript 编译通过（零新增错误）。 - 2026年05月02日
CHESS-LLM-009-I18N: 前端国际化 — 添加中国象棋 LLM 及棋林对弈相关 i18n 翻译键。在 **gomoku-web/src/locales/zh-CN.json** 和 **gomoku-web/src/locales/en-US.json** 中新增 21 个翻译 key，分两组：(1) LLM AI 相关 9 个（chessLlmThinking、chessLlmFallbackMove、chessLlmAnalysisTitle、chessLlmSituation、chessLlmMoveReason、chessLlmCriticalMove、chessNeuralDifficulty、chessLlmHealthOk、chessLlmHealthError）；(2) 棋林对弈相关 12 个（chessPlayModeTitle、chessPlayModeEntered、chessAiMoveMsg、chessUserMoveMsg、chessInvalidMoveMsg、chessCheckMsg、chessCheckmateMsg、chessSelectGameChess、chessAiThinkingStatus、chessEnterPlayMode、chessResignGame、chessNewGame）。同步在 **gomoku-web/src/i18n.ts** 的 Messages 接口中追加全部 21 个类型声明。参数占位符统一使用 `{0}` 格式与现有 t() 函数一致。TypeScript 编译零新增错误（已有错误均为预存问题）。 - 2026年05月02日
CHESS-PLAY-001-AgentPlay-State: 状态管理层改造 — 扩展 useAgentPlay 支持中国象棋对弈模式。修改 **gomoku-web/src/composables/useAgentPlay.ts**（净增约7行）：(1) `PlayMode` 联合类型新增 `'chinese-chess'` 值；(2) 新增 `enterChessMode()` 方法，将 playMode 设为 `'chinese-chess'` 并重置 isAIThinking 为 false；(3) `exitPlayMode()` 已是通用重置逻辑（playMode→'chat'、isAIThinking→false、清理所有候选状态），天然支持 `'chinese-chess'` 模式退出，无需额外修改；(4) `useGlobalAgentPlay()` 单例模式不受影响。TypeScript 编译零新增错误（7个预存错误均为其他文件问题）。 - 2026年05月02日
CHESS-PLAY-002-AgentChessPanel: 中国象棋分屏对弈面板 — 在 **gomoku-web/src/components/agent/** 下新建 `AgentChessPanel.vue`（约260行）。类比 `AgentGomokuPanel.vue` 的设计模式，实现完整的象棋对弈面板：(1) **棋盘渲染**：引入 `Board.vue` 渲染10x9棋盘，内部管理棋盘状态（createInitialBoard初始化标准开局），通过 `selectedPiece`/`validMoves` props控制Board的选择和合法走法高亮；(2) **玩家交互**：监听Board的selectPiece/movePiece事件，仅当轮到玩家行棋时（isPlayerTurn）允许操作，否则忽略交互并清空选择；(3) **走法通知**：玩家落子后通过 `moveToDisplayNotation` 生成标准记谱（如「炮二平五」），emit `@userMove` 事件携带 `{from, to, notation}`；(4) **AI落子接口**：暴露 `placeAiPiece(from, to)` 方法，执行AI走法后emit `@aiMove`；(5) **状态查询**：暴露 `getBoard()`（通过 `convertBoardStateToCodes` 转为数值矩阵）、`getMoveHistory()`、`isValidMove(from, to)`、`getCurrentPlayer()`；(6) **游戏控制**：暴露 `resetGame()` 和 `undoLastMove()`（悔棋回退到玩家回合，恢复吃子），UI提供悔棋/新局/认输按钮；(7) **游戏结束检测**：每次走法后通过 `evaluateMoveResult` 检测将死/困毙，游戏结束时emit `@gameOver` 携带 `{winner, reason}`；(8) **UI布局**：顶部工具栏显示状态文本+回合数+控制按钮，棋盘居中占满面板主体区域；(9) **Props**：`playerSide?: 'red'|'black'`（默认 `'red'`）决定玩家执红/黑。所有UI文本使用i18n。TypeScript编译零新增错误，Vite生产构建通过。 - 2026年05月02日
CHESS-PLAY-003-MoveParser: 中国象棋聊天走法解析器 — 在 **gomoku-web/src/games/chinese-chess/** 下新建 `moveParser.ts`（约180行）。实现 `parseChessMoveText(text, board)` 函数，支持两种输入格式：(1) **标准记谱法**（如「炮二平五」「马八进七」）：正则匹配棋子+列号+方向+目标，根据棋子类型区分直线/斜线子，推算 from/to 坐标。红方使用中文数字（col=9-num），黑方使用阿拉伯数字（col=num-1）。斜线子（马/象/士）的目标列即最后一位数字，行偏移由棋子运动模式推算。confidence=1.0。(2) **坐标格式**（如「0,0-1,0」「(0,0)(1,0)」）：正则提取两组(row,col)坐标。confidence=0.9。两种格式均校验坐标范围(0≤row≤9, 0≤col≤8)和起点有棋子。简写格式暂不实现返回null。TypeScript编译零新增错误，全部184个测试通过。 - 2026年05月02日
CHESS-PLAY-004-AgentView-Layout: AgentView集成中国象棋分屏布局 — 修改 **gomoku-web/src/views/AgentView.vue** 和 **gomoku-web/src/components/agent/AgentChatMessages.vue**。(1) **布局与渲染**：引入 `AgentChessPanel` 组件；`isSplitLayout` 计算属性增加 `playMode === 'chinese-chess'` 条件；在右侧面板增加 `v-else-if="playMode === 'chinese-chess'"` 分支渲染 `AgentChessPanel`，绑定 `@userMove`、`@aiMove`、`@gameOver` 事件；返回按钮和拖拽分割线的 v-if 条件同步增加 `'chinese-chess'` 判断；新增 `chessPanelRef` 模板引用和 `enterChessMode`/`isAIThinking` 从 `useGlobalAgentPlay` 解构。(2) **游戏选择器更新**：`handleGameSelection` 方法增加 `gameType === 'chinese-chess'` 分支，调用 `enterChessMode()` 和 `chessPanelRef.resetGame()`，推送使用 `chessPlayModeEntered` i18n key 的提示消息。(3) **游戏选择按钮启用**：在 `AgentChatMessages.vue` 中移除中国象棋按钮的 `disabled` 属性和灰色样式，改为可点击的 indigo 按钮，绑定 `@click` emit `select-game` 传递 `'chinese-chess'`，按钮文本改用 `chessPlayModeTitle`（不含"即将推出"后缀）。(4) **事件桩处理**：添加 `handleChessUserMove`、`handleChessAiMove`、`handleChessGameOver` 三个事件处理方法（后续 story 将完善 AI 调用逻辑）。Vite生产构建通过，无新增TypeScript错误。 - 2026年05月02日
CHESS-PLAY-005-UserMove-Flow: 中国象棋用户落子拦截与AI思考触发 — 修改 **gomoku-web/src/views/AgentView.vue** 和 **gomoku-web/src/components/agent/AgentChessPanel.vue**。(1) **用户落子事件处理**：重写 `handleChessUserMove` 为 async 方法，用户落子后在聊天流追加玩家行棋消息（chessUserMoveMsg），设置 `isAIThinking=true` 锁定棋盘UI，同时设置 `isThinking=true` 显示思考指示器（chessLlmThinking），从 `chessPanelRef` 获取棋盘数值矩阵和当前玩家，构造 `ChessLLMMoveRequest` 调用 `chessLlmApi.generateMove()` 发起后端LLM走子请求。(2) **走法历史转换**：新增 `pieceToCode` 辅助函数将 PieceType+PlayerSide 枚举映射为数值编码（红方1-7，黑方8-14），`convertMoveHistory` 函数将 MoveHistory[] 转换为 ChessLLMMoveRecord[] 格式。(3) **AI响应处理**：LLM返回后，合并 situationAnalysis 和 reason 为可折叠推理内容（reasoningContent），使用 ThinkingProcess UI 展示思考过程；调用 `chessPanelRef.value.placeAiPiece(from, to)` 执行AI走法；检测将军状态追加 chessCheckMsg 提示；重置 isAIThinking 和思考状态。(4) **AgentChessPanel增强**：`placeAiPiece` 返回类型从 void 改为 `{check, gameOver}|null`，使调用方能检测将军和游戏结束状态。(5) **错误处理**：LLM请求失败时追加错误提示消息，重置所有思考状态。(6) **handleChessAiMove简化**：改为空操作，避免与 handleChessUserMove 中的AI消息重复。Vite生产构建通过，无新增TypeScript错误。 - 2026年05月02日
